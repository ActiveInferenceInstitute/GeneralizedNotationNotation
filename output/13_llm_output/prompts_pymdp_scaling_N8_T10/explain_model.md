# EXPLAIN_MODEL

This repository contains information on generating a randomized active inference model (AI-inference) based on the given dataset of GNNs and PyMDP Scaling models. The generated AI-inference model is annotated with key concepts such as **model name**, **state space block**, **connections** (`A`, `B`), **initial parameterization** (`G`, `F`), **connected components** (`o_m0, o_m1, π_c0, π_c1`) and **actions/controls (u_c0, π_c0)**. The generated model is annotated with key concepts such as **model name**, **state space block**, **connections`, **initial parameterization** (`G`), **connected components** (`o_m0, o_m1, π_c0, π_c1`) and **actions/controls (u_c0, π_c0)**. The generated model is annotated with key concepts such as **model name**, **state space block**, **connections`, **initial parameterization** (`G`), **connected components** (`o_m0, o_m1, π_c0, π_c1`) and **actions/controls (u_c0, π_c0)**. The generated model is annotated with key concepts such as **model name**, **state space block**, **connections`, **initial parameterization** (`G`), **connected components** (`o_m0, o_m1, π_c0, π_c1`) and **actions/controls (u_c0, π_c0)**. The generated model is annotated with key concepts such as **model name**, **state space block**, **connections`, **initial parameterization** (`G`), **connected components** (`o_m0, o_m1, π_c0, π_c1`) and **actions/controls (u_c0, π_c0)**. The generated model is annotated with key concepts such as **model name**, **state space block**, **connections`, **initial parameterization** (`G`), **connected components** (`o_m0, o_m1, π_c0, π_c1`) and **actions/controls (u_c0, π_c0)**. The generated model is annotated with key concepts such as **model name**, **state space block**, **