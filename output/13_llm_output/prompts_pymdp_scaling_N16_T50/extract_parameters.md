# EXTRACT_PARAMETERS

Based on the provided information, here are the key steps to generate a structured annotation:

1. **Generate a list of annotated entities**: Use `generate_ontology` to create a list of annotated entities for each type of entity (e.g., "node", "action"), and then use `create_annotation`.
2. **Create a list of annotations**: Use `create_annotations`, which creates an annotation based on the provided model matrices, B matrix, C matrix, D matrix, and other parameters.
3. **Generate a list of annotated entities for each type of entity**: Use `generate_ontology` to create a list of annotated entities for each type of entity (e.g., "node", "action"), and then use `create_annotation`.
4. **Create a list of annotations**: Use `create_annotations`, which creates an annotation based on the provided model matrices, B matrix, C matrix, D matrix, and other parameters.
5. **Generate a list of annotated entities for each type of entity**: Use `generate_ontology` to create a list of annotated entities for each type of entity (e.g., "node", "action"), and then use `create_annotation`.
6. **Create a list of annotations**: Use `create_annotations`, which creates an annotation based on the provided model matrices, B matrix, C matrix, D matrix, and other parameters.
7. **Generate a list of annotated entities for each type of entity**: Use `generate_ontology` to create a list of annotated entities for each type of entity (e.g., "node", "action"), and then use `create_annotation`.
8. **Create a list of annotations**: Use `create_annotations`, which creates an annotation based on the provided model matrices, B matrix, C matrix, D matrix, and other parameters.
9. **Generate a list of annotated entities for each type of entity**: Use `generate_ontology` to create a list of annotated entities for each type of entity (e.g., "node", "action"), and then use `create_annotation`.