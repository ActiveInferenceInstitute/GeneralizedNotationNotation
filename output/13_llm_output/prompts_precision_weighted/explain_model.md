# EXPLAIN_MODEL

Here's a concise overview of the annotated ontology:

**Annotation:**
- "Predicate" annotation for "Action": A set of actions that are available and what they represent (e.g., "action", "control")
- "Probability" annotation for "Probabilities": A probability distribution over actions, which captures the likelihood of each action being taken in a given state
- "State" annotation for "States": A list of states or actions that are available and what they represent (e.g., "states", "actions")
- "Action" annotation for "Actions": A set of actions that are available and what they capture (e.g., "action"), which captures the action being taken in a given state
- "Prediction" annotation for "Predictions": A list of predictions or beliefs over states, which represent the next state after each prediction
- "ActionProbabilities" annotation for "ActionsProbabilities": A probability distribution over actions that are available and what they do (e.g., "action", "control"), which captures the likelihood of each action being taken in a given state
- "PredictionState" annotation for "PredictionsStates": A list of predictions or beliefs over states, which represent the next state after each prediction
- "ActionProbabilities" annotation for "ActionsProbabilities": A probability distribution over actions that are available and what they do (e.g., "action", "control"), which captures the likelihood of each action being taken in a given state