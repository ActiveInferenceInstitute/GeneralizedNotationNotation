# EXTRACT_PARAMETERS

Here's a systematic parameter breakdown for the GNN example:

**Model Matrices:**

1. **A matrices**:
   - A matrix representing the initial state space dimensions and their interpretation (e.g., `state_space`).
   - A matrix representing the initial observation space dimensions (`observation`) with dimensionality of `observable`.
   
   - A matrix representing the action space dimensions (`action`), which are represented as a list of tuples containing the current agent's state, the next available agent's state, and the next available agent's state.

2. **B matrices**:
   - A matrix representing the initial parameter values (e.g., `initial_param`) for each agent.
   
   - A matrix representing the initial action space dimensions (`action`), which are represented as a list of tuples containing the current agent's state, the next available agent's state, and the next available agent's state.

3. **C matrices**:
   - A matrix representing the initial parameter values (e.g., `initial_param`) for each agent.
   
   - A matrix representing the initial action space dimensions (`action`), which are represented as a list of tuples containing the current agent's state, the next available agent's state, and the next available agent's state.

4. **D matrices**:
   - A matrix representing the initial parameter values (e.g., `initial_param`) for each modality.
   
   - A matrix representing the initial action space dimensions (`action`), which are represented as a list of tuples containing the current agent's state, the next available agent's state, and the next available agent's state.

5. **D matrices**:
   - A matrix representing the initial parameter values (e.g., `initial_param`) for each modality.
   
   - A matrix representing the initial action space dimensions (`action`), which are represented as a list of tuples containing the current agent's state, the next available agent's state, and the next available agent's state.

6. **D matrices**:
   - A matrix representing the initial parameter values (e.g., `initial_param`) for each modality.
   
   - A matrix representing the initial action space dimensions (`action`), which are represented as a list of tuples containing the current agent's state, the next available agent's state, and the next available agent's state.

7. **D matrices**:
   - A matrix representing the initial parameter values (e.g., `