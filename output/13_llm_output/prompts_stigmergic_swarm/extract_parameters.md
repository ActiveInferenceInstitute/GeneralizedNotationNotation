# EXTRACT_PARAMETERS

Based on the document, here are the key aspects of active inference:

1. **Model Representation**: The document provides a detailed overview of how active inference is performed using GNN models and their associated parameter representations. It describes the different types of parameters used in the model (e.g., action-based, probabilistic graphical model), as well as the role of each parameter within the framework.

2. **Model Parameters**: The document provides a systematic breakdown of the model parameters used for active inference:
   - **Initialization parameters** are described and summarized with tabular format.
   - **Parameter file formats recommendations**.
   - **Temporal parameters** are provided in tabular format, along with their corresponding parameter values.

3. **Model Parameters**: The document provides a systematic breakdown of the model parameters used for active inference:
   - **Initialization parameters** are described and summarized with tabular format.
   - **Parameter file formats recommendations**.
   - **Temporal parameters** are provided in tabular format, along with their corresponding parameter values.

4. **Model Statistics**: The document provides a systematic breakdown of the model statistics used for active inference:
   - **Initialization parameters** are described and summarized with tabular format.
   - **Parameter file formats recommendations**.
   - **Temporal parameters** are provided in tabular format, along with their corresponding parameter values.

5. **Model Parameters**: The document provides a systematic breakdown of the model parameters used for active inference:
   - **Initialization parameters** are described and summarized with tabular format.
   - **Parameter file formats recommendations**.
   - **Temporal parameters** are provided in tabular format, along with their corresponding parameter values.

6. **Model Statistics**: The document provides a systematic breakdown of the model statistics used for active inference:
   - **Initialization parameters** are described and summarized with tabular format.
   - **Parameter file formats recommendations**.
   - **Temporal parameters** are provided in tabular format, along with their corresponding parameter values.

7. **Model Statistics**: The document provides a systematic breakdown of the model statistics used for active inference:
   - **Initialization parameters** are described and summarized with tabular format.
   - **Parameter file formats recommendations**.
   - **Temporal parameters** are provided in tabular format, along with their corresponding parameter values.

8. **Model Statistics**: The document provides a systematic breakdown of the model statistics used for active inference:
   - **Initialization parameters** are described