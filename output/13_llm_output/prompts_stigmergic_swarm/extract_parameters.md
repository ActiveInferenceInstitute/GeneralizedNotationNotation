# EXTRACT_PARAMETERS

Based on the document, here are the key parameters for Active Inference (AI) in GNN:

1. **Model Annotation**:
   - **GNNVersionAndFlags**
    - `version`: version of AI model and flags used to generate predictions
    - `flags`: list of annotations that can be generated by the model, including
    *modelAnnotations*: list of annotation types (e.g., "state", "observation")
    - `num_agents`
   - **InitialParameterization**
      - `initialParamList`: list of initial parameters for each agent
       - `initialState`: state space dimensions and structure
       - `initialObservation`: observation dimensionality, including position
     - `initialAction**: action type (e.g., "action", "observation")
     - `initialLocation`: location dimensionality
   - **InitialParameterization**
      - `initialParamList`
    - **InitialParameterization**
        - `initialState`, `initialObservation`, and `initialLocation`
2. **ModelAnnotation**:
   - **GNNVersionAndFlags**
       - `version`: version of AI model and flags used to generate predictions
       - `flags`: list of annotations that can be generated by the model, including
        *modelAnnotations*: list of annotation types (e.g., "state", "observation")
    - `initialState`, `initialObservation`, and `initialLocation`
3. **ModelAnnotation**:
   - **GNNVersionAndFlags**
       - `version`: version of AI model and flags used to generate predictions
       - `flags`: list of annotations that can be generated by the model, including
        *modelAnnotations*: list of annotation types (e.g., "state", "observation")
    - `initialState`, `initialObservation`, and `initialLocation`
4. **ModelAnnotation**:
   - **GNNVersionAndFlags**
       - `version`: version of AI model and flags used to generate predictions
       - `flags`: list of annotations that can be generated by the model, including
        *modelAnnotations*: list of annotation types (e.g., "state", "observation")
    - `initialState`, `initialObservation`, and `initialLocation`
5. **ModelAnnotation**:
   - **GNNVersionAndFlags**
       - `version`: version of AI model and flags used to generate predictions
       - `flags`: list of annotations that