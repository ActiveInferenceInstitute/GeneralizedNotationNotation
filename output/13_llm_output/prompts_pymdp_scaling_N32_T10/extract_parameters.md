# EXTRACT_PARAMETERS

Based on the documentation, here are the steps to generate a structured bibliography of all annotations:

1. **Generate a list of annotated sources**: Use the `generate_sources` function to generate a list of annotated sources for each annotation type (e.g., "ontology", "model"), and then use the `get_annotations()` method to retrieve them from the database.
2. **Create a dictionary with annotations as keys, and source types as values**: Use the `generate_sources` function again to generate a list of annotated sources for each annotation type (e.g., "ontology", "model"), and then use the `get_annotations()` method to retrieve them from the database.
3. **Create a dictionary with annotations as keys, and source types as values**: Use the `generate_sources` function again to generate a list of annotated sources for each annotation type (e.g., "ontology", "model"), and then use the `get_annotations()` method to retrieve them from the database.
4. **Create a dictionary with annotations as keys, and source types as values**: Use the `generate_sources` function again to generate a list of annotated sources for each annotation type (e.g., "ontology", "model"), and then use the `get_annotations()` method to retrieve them from the database.
5. **Create a dictionary with annotations as keys, and source types as values**: Use the `generate_sources` function again to generate a list of annotated sources for each annotation type (e.g., "ontology", "model"), and then use the `get_annotations()` method to retrieve them from the database.
6. **Create a dictionary with annotations as keys, and source types as values**: Use the `generate_sources` function again to generate a list of annotated sources for each annotation type (e.g., "ontology", "model"), and then use the `get_annotations()` method to retrieve them from the database.
7. **Create a dictionary with annotations as keys, and source types as values**: Use the `generate_sources` function again to generate a list of annotated sources for each annotation type (e.g., "ontology", "model"), and then use the `get_annotations()` method to retrieve them from the database.
8. **Create a dictionary with annotations as keys, and source types as values**: Use the `generate_sources` function again to generate a list of annotated sources for each annotation type (e