# Specification: Iroh

## Design Requirements
This module (`iroh`) maps structural logic to the overall execution graph.
It ensures that `Iroh` tasks resolve without runtime dependency loops.

## Components
Expected available types: No specific classes exported.
