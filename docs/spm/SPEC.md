# Specification: Spm

## Design Requirements
This is a documentation-only directory: no `spm` module exists in `src/gnn/`. SPM is not a render or execution framework — it is not an entry in `src/gnn/render/framework_registry.py` — and no SPM code exists anywhere in the tree. The documents here are research/integration notes only.

## Components
Expected available types: No specific classes exported.
