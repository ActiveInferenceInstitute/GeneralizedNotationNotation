# =============================================================================
# CORE DEPENDENCIES (Essential for basic functionality)
# =============================================================================
# This file is maintained for compatibility but UV uses pyproject.toml as the source of truth
# For UV users: Use 'uv sync' instead of 'pip install -r requirements.txt'

# Core scientific computing (lightweight)
numpy>=1.21.0
matplotlib>=3.5.0
networkx>=2.6.0
pandas>=1.3.0
pyyaml>=6.0
scipy>=1.7.0
scikit-learn>=1.0.0

# System monitoring and utilities
psutil>=5.8.0
toml>=0.10.0
defusedxml>=0.7.0

# HTTP and async communication
httpx>=0.27.0

# GNN processing
markdown>=3.0.0

# Active Inference ecosystem
pymdp>=0.0.1

# High-performance computing (essential for execution)
jax[cpu]>=0.4.0
jaxlib>=0.4.0
flax>=0.7.0
optax>=0.1.0

# Visualization and diagramming
discopy[matrix]>=1.0.0
altair>=5.0.0
plotly>=6.2.0

# =============================================================================
# DEVELOPMENT DEPENDENCIES (Install with UV: uv sync --extra dev)
# =============================================================================
# These are defined in pyproject.toml [project.optional-dependencies.dev]
# and [tool.uv.dev-dependencies] for UV users

# Testing framework
# pytest>=6.0.0
# pytest-timeout>=2.1.0
# pytest-asyncio>=0.23.0
# pytest-cov>=3.0.0
# pytest-json-report>=1.5.0
# pytest-xdist>=2.5.0
# pytest-mock>=3.10.0
# pytest-benchmark>=4.0.0
# pytest-sugar>=0.9.0

# Code quality
# black>=22.0.0
# flake8>=4.0.0
# mypy>=0.950
# isort>=5.10.0
# pre-commit>=2.20.0
# mypy-extensions>=1.0.0

# Security tools
# bandit>=1.7.0
# safety>=2.3.0

# Documentation
# sphinx>=5.0.0
# sphinx-rtd-theme>=1.0.0
# myst-parser>=0.18.0

# Development tools
# ipython>=8.0.0
# ipdb>=0.13.0
# jupyter>=1.0.0
# ipywidgets>=7.6.0
# py-spy>=0.3.0
# memory-profiler>=0.60.0

# =============================================================================
# OPTIONAL HEAVY PACKAGES (Install with UV: uv sync --extra [group])
# =============================================================================
# These are defined in pyproject.toml [project.optional-dependencies] for UV users

# === MACHINE LEARNING & AI ===
# torch>=1.12.0         # ~1.5GB - PyTorch (CPU version available)
# transformers>=4.20.0  # ~500MB - Hugging Face transformers

# === LLM INTEGRATION ===
# openai>=1.0.0         # ~50MB - OpenAI API client
# anthropic>=0.5.0      # ~30MB - Anthropic API client

# === ADVANCED VISUALIZATION ===
# seaborn>=0.11.0       # ~100MB - Statistical visualization
# bokeh>=2.4.0          # ~150MB - Web-based visualization
# holoviews>=1.14.0     # ~100MB - High-level plotting
# panel>=0.13.0         # ~80MB - Dashboard framework

# === AUDIO PROCESSING ===
# librosa>=0.10.1       # ~300MB - Audio analysis
# soundfile>=0.12.1     # ~50MB - Audio file I/O
# pedalboard>=0.6.0     # ~200MB - Audio effects

# === GRAPH VISUALIZATION ===
# igraph>=0.9.0         # ~100MB - Graph algorithms
# graphviz>=0.19.0      # ~50MB - Graph visualization

# === RESEARCH TOOLS ===
# jupyter>=1.0.0        # ~500MB - Jupyter notebook
# ipywidgets>=7.6.0     # ~100MB - Interactive widgets

# =============================================================================
# UV USAGE NOTES
# =============================================================================
# 
# For UV users (recommended):
#   uv sync                    # Install core dependencies
#   uv sync --extra dev        # Install development dependencies
#   uv sync --extra llm        # Install LLM dependencies
#   uv sync --extra visualization  # Install visualization dependencies
#   uv sync --extra audio      # Install audio dependencies
#   uv sync --extra gui        # Install GUI dependencies
#   uv sync --extra all        # Install all optional dependencies
#
# For pip users (legacy):
#   pip install -r requirements.txt
#   pip install -e .[dev]      # Install development dependencies
#   pip install -e .[llm]      # Install LLM dependencies
#   # etc.