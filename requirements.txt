# =============================================================================
# CORE DEPENDENCIES (Essential for basic functionality)
# =============================================================================

# Core scientific computing (lightweight)
numpy>=1.21.0
matplotlib>=3.5.0
networkx>=2.6.0
pandas>=1.3.0
pyyaml>=6.0
pathlib2>=2.3.0; python_version < "3.4"
scipy>=1.7.0
scikit-learn>=1.0.0

# Testing and development (essential)
pytest>=6.0.0
pytest-cov>=3.0.0
pytest-asyncio>=0.23.0
pytest-json-report>=1.5.0
pytest-xdist>=2.5.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950

# System monitoring and utilities
psutil>=5.8.0
toml>=0.10.0
defusedxml>=0.7.0

# HTTP and async communication
aiohttp>=3.9.0
httpx>=0.27.0

# GNN processing
markdown>=3.0.0

# Security tools
bandit>=1.7.0
safety>=2.3.0

# =============================================================================
# OPTIONAL HEAVY PACKAGES (Install separately if needed)
# =============================================================================

# Active Inference ecosystem
pymdp>=0.0.1

# High-performance computing (essential for execution)
jax[cpu]>=0.4.0
jaxlib>=0.4.0

# Visualization and diagramming
discopy[matrix]>=1.0.0
altair>=5.0.0

# Uncomment additional heavy packages below based on your needs:

# === MACHINE LEARNING & AI (Heavy packages) ===
# torch>=1.12.0         # ~1.5GB - PyTorch (CPU version available)
# flax>=0.7.0           # ~150MB - JAX neural network library
# optax>=0.1.0          # ~50MB - JAX optimization library

# === LLM INTEGRATION (API clients) ===
# openai>=1.0.0         # ~50MB - OpenAI API client
# anthropic>=0.5.0      # ~30MB - Anthropic API client
# ollama>=0.3.0           # ~small - Local LLM client for Ollama (optional)
# python-dotenv>=1.0.0    # ~small - Load env vars for LLM configs (optional)

# === ADVANCED VISUALIZATION (Heavy) ===
# plotly>=5.0.0         # ~200MB - Interactive plotting
# seaborn>=0.11.0       # ~100MB - Statistical visualization
# bokeh>=2.4.0          # ~150MB - Web-based visualization
# holoviews>=1.14.0     # ~100MB - High-level plotting
# panel>=0.13.0         # ~80MB - Dashboard framework

# === AUDIO PROCESSING (Heavy) ===
# librosa>=0.9.0        # ~300MB - Audio analysis
# soundfile>=0.10.0     # ~50MB - Audio file I/O
# pedalboard>=0.5.0     # ~200MB - Audio effects

# === GRAPH VISUALIZATION ===
# igraph>=0.9.0         # ~100MB - Graph algorithms
# graphviz>=0.19.0      # ~50MB - Graph visualization

# === RESEARCH TOOLS ===
# jupyter>=1.0.0        # ~500MB - Jupyter notebook
# ipywidgets>=7.6.0     # ~100MB - Interactive widgets

# === ACTIVE INFERENCE ===
# pymdp>=0.0.1          # ~50MB - Active inference library

# === SAPF (Audio backend) ===
# sapf                  # Temporarily unavailable in PyPI