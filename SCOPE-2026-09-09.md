# SCOPE-2026-09-09 — GNN repo improvement spec (review-only phase)

**Author**: overnight review agent (scope-campaign, repo #1 of 9).
**Phase**: review + TODO truth + scoping ONLY. Nothing here is implemented.
**Tree state at review time**: branch `improvements/2026-09-08-paired-revision-fep-lean`
(5 ahead / 231 behind local `main`; PRs #77–#82 merged upstream after this
branch forked — expect a non-trivial rebase before any of this work starts).
Working tree clean before this document; only `TO-DO.md` and this file were
modified, per campaign contract.

## §Verification-Run-2026-09-09 (evidence baseline)

Probes executed during this review (results cited by items below):

| Probe | Command | Result |
|---|---|---|
| ruff check | `uv run --extra dev ruff check src/gnn scripts` | pass (all clear) |
| ruff format | `ruff format --check src/gnn scripts` | 619 files already formatted |
| ruff F401/F811 | `ruff check --select F401,F811 --statistics` | **84** findings (75 F401 + 9 F811), policy-ignored |
| mypy | `uv run --extra dev mypy src --config-file pyproject.toml` | Success: 0 issues in 588 files |
| default suite | `pytest tests/ -q -n 4 --ignore=tests/llm/test_llm_ollama*.py` | **3 failed, 4821 passed, 17 skipped** in 347s (see SC-10) |
| doc/skill gates | docs_audit, doc-patterns, doc-terms, terminology, capability-contracts, skills-health (all `--strict`) | all pass |
| v3 acceptance | `scripts/run_v3_orchestration_acceptance.py --strict` | 19/19 checks passed |
| bandit (med/med) | `bandit -r src/gnn -c pyproject.toml` | no medium+ findings under current config (but see SC-2 on config) |
| test-estate census | grep/AST count | 377 test files, 4,171 `def test_`, 1 conftest (305 ln); 145 weak-assertion-only tests (SC-43); ~4,817 ln of tests never collected by pytest (SC-42) |

Known-verified external facts used below: 15+ `validate_gnn*` defs and 20
`process_*_mcp` wrappers counted by grep; six >2000-line modules re-measured
with `wc -l`; `__version__` drift counted by grep across `src/gnn`.

## §Review-baseline-and-main-delta (pinned 2026-09-09)

**Review baseline**: branch `improvements/2026-09-08-paired-revision-fep-lean`
at `a96d0e538` (5 ahead of merge-base `3726ed4ef`). Every probe in
§Verification-Run and every file:line in the items below was measured on that
branch. **Re-baselined 2026-09-09 against current `origin/main` `222ffe8b8`**
(upstream PRs #77–#99 merged; this worktree = main + the 5 fep_lean
paired-revision commits): the table below is the authoritative main-state
per item, re-verified file-by-file in the worktree before writing.

| Item | State on main (222ffe8b8) | Measured evidence |
|---|---|---|
| SC-6 / MAJ-04 | **DONE upstream** — all six modules decomposed, receipt recorded | TO-DO.md receipt: visualizer 2871→283 (PR #40), visualizations 2412→58 (PR #29), test_round_trip 2214→1356 (PR #43), jax_renderer 2200→170 (PR #33), translator 2150→303 (PR #34), analyzer 2031→263 (PR #32); facade per-file F401 ignores (`pyproject.toml:456`+); coverage floor 50→60 |
| TODO ruff-audit item | **DONE upstream** | global `F401`/`F811` ignores gone; per-file-ignores hold the six facades, `round_trip_availability`, `visualizer_style`, `execute/processor.py`, and the guarded `parsers/*` probes (`pyproject.toml:456-470`+); `ruff check --select F401,F811 src/gnn` reports 0 findings (TO-DO.md receipt, suite 4285 passed) |
| SC-10 | **DONE upstream** | `TestUtilsMigrationHelper` deleted — 0 matches for `TestUtilsMigrationHelper`/`migration_helper` under `tests/` |
| ml-ai/torch extras parity (TODO parity item) | **RESOLVED upstream** | `uv sync --extra dev --extra ml-ai --extra torch --frozen` un-skips the 12 env-skipped tests; `just test-extras` (justfile:42) + ci.yml `extras` PR-time job (ci.yml:185) |
| utils/pipeline_validator near-name collision | **RESOLVED upstream** | runtime validator renamed to `gnn/pipeline/pipeline_runtime_validator.py` with a `DeprecationWarning` shim at the old path; contract pinned in `tests/pipeline/test_pipeline_runtime_validator.py` |
| stale singular doc paths | **RESOLVED upstream** | 21 occurrences re-pointed to real homes; regression gate `scripts/check_doc_path_references.py` strict and CI-wired (local-gates) |
| SC-41 | **live evasions closed upstream; one residual token gap** | `FORBIDDEN_SKIP_TOKENS` gained bare `pytest.mark.skip`/`xfail` + unittest forms (`tests/test_zero_skip_contracts.py:50-63`) — but **not** bare `pytest.mark.skipif`, so `pytestmark = pytest.mark.skipif(...)` still evades; one-line follow-up |
| SC-13 (partial) | parity items landed | `justfile` test-cov now `-m "not pipeline and not mcp"` (justfile:36); `just test-extras` (justfile:42); ci.yml `extras` PR-time job. Remaining: `just quality` gate additions, format-check scope, MCP-floor single-sourcing |
| SC-15 (partial) | silent fallback removed | `utils/pipeline.py:get_output_dir_for_script` is now a clean thin delegate to `gnn.pipeline.config` (`utils/pipeline.py:23-40`; no try/except). Remaining: the module itself, the dual logging entry (`get_pipeline_utilities` still wires `setup_step_logging`, `utils/pipeline.py:57-71,123-125`; `utils/logging_utils.py` vs `utils/logging/logging_utils.py`), and the `pipeline_template` upward import (`utils/pipeline_template.py:15`) |
| SC-25 | **RESOLVED upstream** | `utils/network_utils.py` removed in the wave-2 dead-module sweep (W2-02); the remaining httpx egress (`mcp/sympy_mcp_client.py:79`) constructs its client with `timeout=30.0` |
| SC-7 / MAJ-05 | still open (18 defs measured on main) | `grep -rE "def validate_gnn" src/gnn` = 18 |
| SC-8 / MAJ-06 | **DONE upstream** — collapsed onto the generic dispatcher | 18 of the 19 `process_*_mcp` wrapper files route through `gnn.utils.mcp_dispatch.run_pipeline_step_mcp` / `run_tool_envelope` (e.g. `src/gnn/analysis/mcp.py:20`); the 19th (`src/gnn/sapf/mcp.py`) stays bespoke by design and `src/gnn/doc/mcp.py` a documented no-op auto-discovery stub — both recorded in the CHANGELOG §Unreleased MAJ-06 follow-up bullet |
| SC-42 | still open (5 orphan files on main) | `src/gnn/testing/test_round_trip.py` 1349, `test_comprehensive.py` 1005, `test_integration.py` 197, `test_xml_parser_only.py` 137, `src/gnn/utils/test_utils.py` 1247 = 3,935 ln never collected (`pytest.ini:16` `testpaths = tests` only) |
| Unchanged on main | SC-5 (Dockerfile `uv pip install . \|\| true` :31, unpinned jax :33, no `.dockerignore`), SC-19 (scripts inventory), SC-3/SC-20/SC-21/SC-23 (token-gate scripts; `manuscript_build_figures.py:178` still regenerates only-when-missing), SC-24 docs drift (`README.md:85,103` "32 module directories" vs 45 measured; `SECURITY.md` table tops at 3.2.x), SC-16/17/26/27/29/33 | verified in worktree |

**Upstream-scoped open work already pinned in main's TO-DO.md** (additive to
the scope-campaign items): pipeline-orchestration rows W2-D1..W2-D7,
W2-M1..W2-M4, W2-J1, and the MCP/execute remainders MED-03b, MED-04, MIN-01
(see TO-DO.md "Deep horizon wave 2" sections).

**Consequence for the execution order**: on current main, SC-6, SC-10, the
ruff-audit item, SC-25, and SC-8 drop out; SC-41 shrinks to a one-line
follow-up (add the bare `pytest.mark.skipif` token); SC-13 and SC-15 shrink
to residual scope; the remaining items carry over and are repopulated as
pinned rows in TO-DO.md "Open Scoped Roadmap" (SC-1, SC-2, SC-3, SC-16,
SC-17, SC-24, SC-26, SC-27, SC-29, SC-33, SC-41 residual, SC-42, SC-43).
Re-run §Verification-Run on the target tree before starting any item.

---

## Major items

### SC-1 — MCP execution path bypasses the security gate and sandbox
- **Severity**: major | **Category**: security
- **Motivation**: `execute/mcp.py:132` (`execute_gnn_model` MCP tool) dispatches through `GNNExecutor` (`execute/executor.py:19-75`) whose per-framework runners never reference `scan_script_for_execution` or the sandbox — grep over all `execute/*/` runner dirs is empty. The wired gate lives only on the other execution stack: `execute/processor.py:1033-1051` (`SecurityGateBlocked` before every Step 12 run). Two parallel subprocess envelopes for the same job: `execute/processor.py:993-1446` vs `executor.py:1089-1176` plus hand-rolled runner envelopes (~400+ duplicated lines across `activeinference_runner.py:72-325`, `rxinfer_runner.py:107-128`, `stan_runner.py:59-63`, `lean_runner.py:108-113`). The only remotely invocable execution path is the ungated one.
- **Scope**: route `execute_gnn_model_mcp` (and ideally all `GNNExecutor` dispatch) through `execute_single_script`, or insert the pre-exec gate + sandbox into `GNNExecutor`; then collapse the duplicated runner envelopes onto one shared envelope. Keep tool names/signatures stable (MCP registry unchanged).
- **Acceptance probe**: `uv run --extra dev pytest tests/execute tests/mcp -q` plus a new negative test: an unsafe rendered script submitted via the MCP execute tool returns `SecurityGateBlocked`, not a run. `PYTHONPATH=src uv run --extra dev python scripts/run_v3_orchestration_acceptance.py --strict` stays green.
- **Effort**: L | **Order**: first among majors (security); independent of MAJ-04/05/06.

### SC-2 — Fail closed: security gate, sandbox receipt, codegen fallback, B601 tripwire
- **Severity**: major | **Category**: security
- **Motivation**: (a) `execute/processor.py:1052-1053` — the pre-exec gate catches `ImportError` and continues at debug level: a broken `security` module silently disables the only active enforcement. (b) `execute/sandbox.py:43` + `processor.py:965` — OS sandbox is real code but `_DEFAULT_MODE = "off"`; default enforcement is static scan only, and running unsandboxed is a `logger.warning`, not a receipt. (c) `render/generators.py:170-176` — `_matrix_to_julia` falls back to interpolating raw, unvalidated spec text into generated Julia source when `safe_literal_eval` fails (same pattern in the PyMDP template path); combined with (a) the scan gate is the only barrier, and the MCP path skips it. (d) `pyproject.toml:412` — bandit `skips = ["B101","B601"]` permanently disables the shell-subprocess detector while 84 inline `# nosec B60x` suppressions already remove list-arg checks at those sites; a future `shell=True` edit would pass CI silently (measured: zero `shell=True` today, so nothing real is currently masked).
- **Scope**: (a) hard-block on `ImportError` of the security module; (b) emit a pipeline-visible "running unsandboxed" receipt (and consider requiring explicit `GNN_SANDBOX` opt-out per run manifest); (c) replace the raw-text fallback with a rejected-parse error in both Julia and PyMDP render paths; (d) drop `B601` from bandit skips and reconcile the 84 nosec suppressions (keep justified ones with comments).
- **Acceptance probe**: `uv run --extra dev bandit -r src/gnn -c pyproject.toml -q --severity-level medium --confidence-level medium` exits 0 after fixes; unit tests assert gate failure on a sabotaged `security.processor` import and a rejected unparseable matrix.
- **Effort**: M | **Order**: pairs with SC-1 (same seam); lands before SC-6 envelope work.

### SC-3 — Close the "consistently stale" manuscript-token class
- **Severity**: major | **Category**: tests (gate machinery)
- **Motivation**: The token pipeline can ship consistently-stale JSON+PNG+registry through every gate. Concretely: `scripts/manuscript_build_figures.py:178-188` regenerates `output/data/manuscript_variables.json` **only when missing** — a stale-but-present JSON is baked into fresh PNGs and re-recorded as `consumed_tokens` provenance (`:216-219`). `scripts/check_manuscript_tokens.py:249` recomputes tokens **live** but nothing anywhere compares the committed JSON to a fresh `generate_variables(HEAD)`; the only test reading the committed file compares it to the registry (stale-vs-stale, `tests/test_manuscript_figure_freshness.py:51-58`). Hydration failure is silent: `scripts/z_generate_manuscript_variables.py:104-116` exits 0 in "standalone mode" when the template root is missing, and the gate scans `manuscript/*.md` sources only (`check_manuscript_tokens.py:248`), never the hydrated `output/manuscript/` copies — so a PDF with literal `{{TOKEN}}` leaves every in-repo gate green. This is the class behind the historical 365/367 prose-vs-figure drift.
- **Scope**: (1) make `manuscript_build_figures.py` regenerate the variables JSON unconditionally, or fail when its `GNN_GIT_COMMIT` ≠ HEAD; (2) add a gate/test asserting `token_checksum(generate_variables(HEAD)) == token_checksum(committed JSON)` and per-figure `png_sha256 == digest(rebuild)`; (3) wire it as a local-gates step; (4) scan `output/manuscript/` for unresolved `{{TOKEN}}`; (5) make a render-invoked `z_generate` fail (not exit 0) when the template root cannot be found.
- **Acceptance probe**: `uv run --extra dev python scripts/check_manuscript_tokens.py --strict` green after a deliberate count perturbation is **detected** (negative test), and `uv run --extra dev pytest tests/test_manuscript_figure_freshness.py tests/test_manuscript_path_claims.py -q` green.
- **Effort**: M | **Order**: quick win inside the majors; independent.

### SC-4 — Make the documented dev loop work on a fresh clone
- **Severity**: major | **Category**: DX
- **Motivation**: pytest/ruff/mypy exist only in the `dev` **extra** (`pyproject.toml:79-135`); there is no `[dependency-groups]` dev group and no `default-groups` setting. `uv run` syncs project + default groups only, so on a fresh clone `just test` (`justfile:18`), `just lint` (`:46`), `just typecheck` (`:63`) and effectively every recipe in `just quality` (`:110`) fail with missing tools. Three recipes already patch around it with `--extra dev` (`justfile:71,87,91`) — evidence the authors hit this and fixed only the breakage site.
- **Scope**: either add `--extra dev` to every recipe, or move the tool deps into a real `[dependency-groups] dev` and set `[tool.uv] default-groups = ["dev"]`. Prefer the latter (single point of truth).
- **Acceptance probe**: from a fresh `uv venv` clone: `uv run pytest --version && just test --collect-only` (or equivalent) succeeds without a prior `just setup`.
- **Effort**: S | **Order**: quick win; unblocks all other contributors.

### SC-5 — Fix the Docker test image (broken defaults, false-green install, unpinned JAX)
- **Severity**: major | **Category**: packaging
- **Motivation**: `Dockerfile:31` — `RUN uv pip install . || true` swallows a failed project install: the image can build "green" with no `gnn` package installed. `Dockerfile:43` — default CMD `python -m pytest -n0 …` requires pytest-xdist, which `:32` does not install ("unrecognized arguments: -n0" out of the box), and `--cov=src` contradicts `--cov=gnn` used everywhere else (`justfile:36`, `ci.yml:135`). `Dockerfile:33` — `uv pip install jax jaxlib` is unpinned and bypasses the `<0.11` upper bound the lock enforces (`pyproject.toml:62-63`). No `.dockerignore`, so `COPY . /workspace` (`:23`) ships `.venv`, caches, and the 6,604-file `output/` tree into the context.
- **Scope**: replace `uv pip install . || true` + ad-hoc installs with `uv sync --frozen --extra dev`; fix the CMD (`-n auto` is fine once xdist arrives via dev; `--cov=gnn`); pin jax or drop the ad-hoc install; add `.dockerignore` (`.venv`, `__pycache__`, `output/`, `out/`, caches).
- **Acceptance probe**: `docker build -t gnn-test . && docker run --rm gnn-test python -m pytest --collect-only -q | tail -1` succeeds.
- **Effort**: S | **Order**: quick win.

### SC-6 — Decompose the six >2000-line modules (MAJ-04, carried + corrected)
- **2026-09-09 main-delta**: **DONE on main** — all six modules already decomposed upstream (283/91/1350/172/301/292 lines measured via `git show main:`); see §Review-baseline-and-main-delta. Kept here because this branch still carries the pre-split code; drop after rebase.
- **Severity**: major | **Category**: architecture
- **Motivation**: TODO MAJ-04, re-measured 2026-09-09: `src/gnn/integration/meta_analysis/visualizer.py` 2871, `analysis/visualizations.py` 2412, `testing/test_round_trip.py` 2214, `render/jax/jax_renderer.py` 2200, `render/discopy/translator.py` 2150, `analysis/analyzer.py` 2031. One correction to the TODO's framing: the duplicated subprocess envelope is **not** in the nine renderers (renderers contain zero subprocess calls — verified by grep); it is the second execution stack in `execute/` (see SC-1). The biggest `print()` clusters also live here (`testing/test_round_trip.py` 148 prints). Second correction: `testing/test_round_trip.py` is a **never-collected test file** living inside the package (`pytest.ini` `testpaths=tests`); see SC-42 before decomposing it.
- **Scope**: as pinned in TO-DO.md (mechanical extraction following the 3.3.0 `execute/processor.py` split pattern; facade re-exports preserved; one module per PR). Fold the SC-1 envelope consolidation into whichever PR touches `testing/`/`analysis/` seams.
- **Acceptance probe**: per module — old import paths still resolve, `uv run --extra dev mypy src/gnn` clean, `just lint && just format-check` clean, module tests green, moved code byte-identical modulo imports.
- **Effort**: XL (six PRs) | **Order**: after SC-1/SC-2 (envelope target exists first).

### SC-7 — De-duplicate the `validate_gnn*` surface (MAJ-05, carried + enlarged)
- **Severity**: major | **Category**: architecture
- **Motivation**: TODO MAJ-05; re-counted 2026-09-09: 15+ defs across 12 modules — `gnn/__init__.py:125` `validate_gnn_file`, `parsers/basic.py:266,276`, `processing/processor.py:276`, `llm/llm_operations.py:347,452`, `mcp/gnn_root.py:102`, `mcp/processors.py:330`, `schema/parser.py:542`, `schema_validator/validator.py:840`, `validation/simple.py:151,165`, `validation/mcp.py:53`, `type_checker/mcp.py:19`, `execute/pymdp/pymdp_utils.py:498`, plus a local `validate_gnn` inside `gui/gui_2/ui.py:874` and `mcp/sympy_mcp_client.py:398`. Six-plus unrelated semantics share the name.
- **Scope**: as pinned in TO-DO.md — rename to unambiguous names with `DeprecationWarning` aliases, one module per PR, MCP tool registry unchanged.
- **Acceptance probe**: one unambiguous `def validate_gnn*` name per semantic; old-name and new-name tests pass; `uv run --extra dev python scripts/check_mcp_skills_health.py --strict` green.
- **Effort**: L | **Order**: independent; good first major after quick wins.

### SC-8 — Collapse the `process_*_mcp` wrappers (MAJ-06, carried + enlarged)
- **Severity**: major | **Category**: architecture
- **Motivation**: TODO MAJ-06; re-counted 2026-09-09: **20** copy-pasted wrappers (`advanced_visualization`, `analysis`, `audio`, `execute`, `export`, `gui`, `integration`, `intelligent_analysis`, `llm`, `ml_integration`, `ontology`, `render`, `report`, `research`, `sapf`, `security`, `validation`, `visualization`, `website` + AGENTS.md-documented variant), all `(target_directory, output_directory, verbose)`. Adjacent dead surface in the same seam: `src/gnn/doc/mcp.py:1-8` (self-described no-op stub for auto-discovery) and `src/gnn/sapf/` (34-line facade with no src caller; test-only consumers).
- **Scope**: one generic dispatcher with per-module registration (as pinned in TO-DO.md); fold the `sapf/` facade and `doc/mcp.py` stub decision into the same PR series.
- **Acceptance probe**: `just skills-health` green; mcp-audit CI job green; per-module MCP tests pass; wrapper files shrink to registration calls.
- **Effort**: L | **Order**: after SC-7 (same deprecation-alias pattern established).

### SC-9 — output/: 6,604 tracked generated files (policy decision required)
- **Severity**: major | **Category**: architecture / DX
- **Motivation**: `.gitignore:78` "Keep it committed!" policy tracks the entire `output/` tree (6,604 files, incl. `output/manuscript`, `output/pdf`, `output/figures`) plus 7 stray `out/` files. Every pipeline/test run can dirty thousands of tracked files (this review had to redirect pytest logs to /tmp to keep the tree clean); CodeQL runs build-mode `none` *because* of these trees (`codeql.yml:41-50`, `codeql-config.yml`); clone weight and merge conflicts are structural.
- **Scope**: NOT a mechanical flip — manuscript gates (`tests/test_manuscript_latex_log.py`, `test_manuscript_published_commands.py`, figure freshness) read committed artifacts, and the manuscript itself is shipped from this tree. Decision needed: keep curated published bundles (`output/manuscript/`, `output/pdf/`, `output/figures/figure_registry.json` + referenced PNGs, `output/data/manuscript_variables.json`) tracked; ignore volatile step outputs (`output/[0-9]*_*_output/`, htmlcov, semantic_fidelity, cross_framework, model_family_acceptance). Document the boundary in `.gitignore` and AGENTS.md; migrate receipts that must stay durable into a small `receipts/` dir if needed.
- **Acceptance probe**: `git status --porcelain` stays empty across a full `uv run --extra dev pytest tests/ -q -n4` run; manuscript gates still green.
- **Effort**: M (mechanics) + owner decision | **Order**: before any CI/gate refactor that touches `output/` (SC-3, SC-22).

---

## Medium items

### SC-10 — Default suite is red: 3 tests assert a deleted module, hidden from CI by a marker
- **2026-09-09 main-delta**: **DONE on main** — `TestUtilsMigrationHelper` deleted upstream (PRs #77–#82). Red-suite state applies to this branch only.
- **Severity**: medium | **Category**: tests
- **Motivation**: Full default suite (this review): **3 failed, 4821 passed, 17 skipped**. All three failures are `tests/pipeline/test_pipeline_infrastructure.py::TestUtilsMigrationHelper` (lines ~204): `ImportError/ModuleNotFoundError: No module named 'gnn.utils.migration_helper'` — the module whose removal the TODO celebrates. CI never sees them because the module sets `pytestmark = [pytest.mark.pipeline, ...]` (`:20`) and CI runs `-m "not pipeline and not mcp"` (`ci.yml:131`). `.pytest_cache/v/cache/lastfailed` additionally holds five stale `src/tests/...` entries from a deleted tree (cache rot; `src/tests/` does not exist).
- **Scope**: delete the `TestUtilsMigrationHelper` class and its module-list entries (the module list at the file head still names `utils.migration_helper`, `utils.pipeline_monitor`, `utils.resource_manager`, `utils.script_validator` — verify each still exists and prune dead entries).
- **Acceptance probe**: `uv run --extra dev python -m pytest tests/ -q --tb=no -n4` reports **0 failed**.
- **Effort**: S | **Order**: quick win, first.

### SC-11 — Pin CI actions to commit SHAs
- **Severity**: medium | **Category**: security / CI
- **Motivation**: all 10 workflows pin mutable major tags (`ci.yml:37,40,45,101`: `actions/checkout@v7`, `setup-python@v7`, `astral-sh/setup-uv@v7`, `upload-artifact@v7`; `dependency-review-action@v5`; `codeql-action/*@v4`; `rhysd/actionlint@v1.7.12`). The repo's stated supply-chain posture (uv constraint floors, Dockerfile pin comment) is undermined: a moved tag lands silently. Dependabot covers version bumps; SHA-pinning is the missing control.
- **Scope**: pin every `uses:` to a full commit SHA (Dependabot keeps comments with the version).
- **Acceptance probe**: `grep -rn "uses:.*@v" .github/workflows/` returns nothing; actionlint job green.
- **Effort**: S | **Order**: quick win.

### SC-12 — full-extras.yml swallows broken optional extras as "skipped"
- **Severity**: medium | **Category**: CI
- **Motivation**: `full-extras.yml:62-82` treats `ImportError` during optional-import validation as `skipped (C-ext build failure)` for gui (gradio/streamlit) and research/scaling (sympy/numba/dask) — a genuinely broken extra prints "skipped" and the weekly gate passes: false green by design for the exact groups the workflow exists to catch. Failure notification is only a `::warning::` annotation (`:98-101`).
- **Scope**: distinguish import errors from build failures (attempt `importlib.metadata` first; only skip on metadata/ABI errors, fail on ImportError from app code); add an issue-filing or persistent-artifact step for Sunday-run failures.
- **Acceptance probe**: sabotage a trivial extra locally → workflow step (or its local equivalent command) exits non-zero.
- **Effort**: S | **Order**: quick win.

### SC-13 — `just quality` blind spot vs CI (three gates missing, scope drift, floor spelled three ways)
- **2026-09-09 main-delta**: partially resolved on main — test-cov is now marker-filtered and the 12 env-skipped tests gained an `extras` CI job + `just test-extras` recipe. Remaining after rebase: `just quality` gate additions, format-check scope, MCP-floor single-sourcing.
- **Severity**: medium | **Category**: DX / CI parity
- **Motivation**: `just quality` (`justfile:110`) omits `capability` (`:75`), `skills-health` (`:71`), and `tokens` (`:79`) — all three are CI-blocking (`ci.yml:86`, `local-gates.yml:50-53`). Local format-check covers `src/gnn scripts`, CI covers `src scripts` (`justfile:59` vs `ci.yml:58`). The MCP tool floor is spelled three ways: `mcp-audit.yml:36` (`> 139`), `ci.yml:175` (`< 140`), `justfile:91` (`>= 140`).
- **Scope**: add the three gates to `just quality`; widen local format-check to `src scripts`; single-source the MCP floor (one constant, e.g. in `tests/mcp/test_mcp_audit.py`, imported by all three surfaces).
- **Acceptance probe**: `just quality` green on a red-`tokens` sabotage (negative check); `grep -c "139\|140" justfile .github/workflows/mcp-audit.yml .github/workflows/ci.yml` shows one definition site.
- **Effort**: S | **Order**: quick win.

### SC-14 — pre-commit drift: ruff three minors behind the lock, hooks unwired in CI, four dead linter configs
- **Severity**: medium | **Category**: code-quality / DX
- **Motivation**: `.pre-commit-config.yaml:9` pins `ruff-pre-commit v0.11.12` while the lock resolves ruff 0.15.12 (`pyproject.toml:435` comments assume 0.14+) — pre-commit reformats differently from `just format`/CI. No workflow runs pre-commit; file-hygiene hooks are advisory-only. `black`/`flake8`/`isort`/`pylint` are pinned in the dev extra (`pyproject.toml:105-112`) with tool configs (`:294-317`) but enforced by nothing — four dead linters whose configs can drift from ruff.
- **Scope**: bump the pre-commit ruff rev to the locked minor; run pre-commit in CI (or fold its unique hooks into a just recipe); delete black/flake8/isort/pylint deps + configs in favor of ruff (ruff format supersedes black; isort `I` rules already selected).
- **Acceptance probe**: `uv run --extra dev pre-commit run --all-files` green and consistent with `just format-check`; `grep -n "black\|flake8\|pylint" pyproject.toml` shows no orphaned config.
- **Effort**: S | **Order**: quick win.

### SC-15 — Collapse `gnn/utils/pipeline.py` silent fallback + double logging entry points
- **2026-09-09 main-delta (verified)**: `get_output_dir_for_script` on main is now a clean thin delegate to `pipeline.config` (docstring documents the lazy import; no try/except, no fallback). Remaining after rebase: the module itself (46 importers on this branch), the dual logging entry, and the `pipeline_template` upward import.
- **Severity**: medium | **Category**: architecture / code-quality
- **Motivation**: `utils/pipeline.py:17-36` re-implements `pipeline.config.get_output_dir_for_script` behind `except (ImportError, Exception)` — a silent second source of truth with 46 inbound importers. `get_pipeline_utilities` still wires `setup_step_logging` (`:72,126`). Logging has two valid import paths (`utils/logging_utils.py` facade with renamed aliases vs `utils/logging/logging_utils.py` impl; 13 files import one, 4 the other). `utils/pipeline_template.py:14` imports `gnn.pipeline.config` upward — latent cycle that only works because `utils/__init__` is lazy. (Now recorded in TO-DO.md "setup_step_logging residue" item.)
- **Scope**: point `utils/pipeline.get_output_dir_for_script` at the canonical implementation (or inline it and delete the module via mechanical import rewrite); keep exactly one logging entry path; break the `pipeline_template` upward import by inlining the two lines it needs.
- **Acceptance probe**: `grep -rn "from gnn.utils.pipeline import" src | wc -l` drops to 0 (or the module is gone); `uv run --extra dev python -c "import gnn.utils.pipeline_template"` works without triggering `gnn.pipeline` first.
- **Effort**: M | **Order**: independent; pairs with SC-16 in a utils-hygiene PR.

### SC-16 — Version constants: 25+ hardcoded `__version__` with real drift
- **Severity**: medium | **Category**: code-quality / packaging
- **Motivation**: grep across `src/gnn`: 24× `__version__ = "3.3.0"` + 1 inside a function, **plus drift**: 7 files at `1.7.0` (research/security/integration/website/ontology), 2 at `1.1.3` (`audio/sapf/__init__.py:35`), 1 at `1.0.0`. `pyproject.toml` is never read programmatically, so every release needs 25+ hand edits and half the subpackage versions are already lies.
- **Scope**: single source (`gnn/__init__.py.__version__` or `importlib.metadata.version("generalized-notation-notation")`); subpackages re-export; stale 1.x values become re-exports of the canonical constant.
- **Acceptance probe**: `grep -rn "__version__ = \"" src/gnn --include="*.py" | grep -v 3\.3\.0 | wc -l` → 0 after; a release-bump dry-run touches 1 file.
- **Effort**: S/M | **Order**: quick win before v4.0.0 work.

### SC-17 — Error-handling hygiene (tiered)
- **Severity**: medium | **Category**: code-quality
- **Motivation**: 1,277 `except Exception` in `src/gnn` (zero bare `except:` — good). 23 `except: pass` swallows, 12 concentrated in `analysis/rxinfer/analyzer.py`. 49 `except Exception → return None` conversions (parsers/yaml_parser.py 9, xml_parser.py 5, ...) let malformed specs propagate invisibly. `utils/error_handling.py:61` — `PipelineError` is a **dataclass, not an Exception**: `raise PipelineError(...)` is a `TypeError` waiting in the central error module. Duplicate `ConversionError` classes with no relation (`parsers/common.py:13` vs `parsers/converters.py:86`). 528 bare `ValueError/RuntimeError` raises vs 27 existing custom exception classes (geo_infer cluster: 30+26+20 sites).
- **Scope**: (1) make `PipelineError` inherit `Exception` (S); (2) dedupe `ConversionError` (S); (3) convert the 12 rxinfer swallows to logged/collected errors (S); (4) adopt domain exceptions at the geo_infer + parser seams, keeping `ValueError` re-exported for compat (M, per-seam PRs). Defer the 1,277 long tail to convention + lint ratchet.
- **Acceptance probe**: `uv run --extra dev python -c "from gnn.utils.error_handling import PipelineError; raise SystemExit(isinstance(PipelineError('x'), Exception))"` prints True; `pytest tests/analysis/test_rxinfer* -q` green with swallows converted.
- **Effort**: M (tiered) | **Order**: items 1-3 are quick wins; item 4 rides SC-6 PRs.

### SC-18 — `print()` → structured logging at pipeline-visible seams
- **Severity**: medium | **Category**: code-quality
- **Motivation**: 834 `print(` calls across 65 files; worst: `testing/test_round_trip.py` 148, `mcp/npx_inspector.py` 43, `render/discopy/translator.py` 38, `pipeline/health_check.py` 32, `cli/__init__.py` 30. Bypasses the structured-logging convention; breaks capture in long-running orchestration.
- **Scope**: convert the pipeline-visible seams first (pipeline/, cli/, mcp/, numbered orchestrators), keeping CLI user-output prints deliberate (flagged `# user-output` or via a dedicated emitter).
- **Acceptance probe**: `grep -rn "print(" src/gnn/pipeline src/gnn/cli | wc -l` → ~0 non-CLI-emitter sites; `just test` green.
- **Effort**: L (tiered) | **Order**: after SC-17; naturally rides SC-6 decompositions.

### SC-19 — Dead and orphaned scripts in `scripts/`
- **Severity**: medium | **Category**: code-quality / DX
- **Motivation**: `scripts/verify_logging_duplication.py` — referenced by nothing (dead). `scripts/verify_pomdp_pipeline.py` — 628-line comprehensive POMDP verifier referenced nowhere while sibling `check_pomdp_gridworld_outputs.py` is the maintained gate (`justfile:83`): two overlapping verifiers, one rotting. `scripts/check_external_links.py` — has a mechanics test but is never run against the live tree in CI/justfile (link rot unguarded; docs_audit covers relative links only). `scripts/add_module_docstrings.py` — a source-mutating codemod sitting beside gates. `scripts/run_pymdp_gnn_scaling_analysis.py` — 1,185-line config-driven experiment orchestrator blurring the "scripts/ = gates" model.
- **Scope**: delete `verify_logging_duplication.py`; either wire `verify_pomdp_pipeline.py` into the gridworld gate story or archive it; add `check_external_links.py` to docs-audit.yml (scheduled, not per-PR — network flakiness); move the codemod + scaling orchestrator under `scripts/experiments/` or `docs/development/`.
- **Acceptance probe**: `for f in scripts/*.py; do grep -q "$f" justfile .github/workflows/*.yml docs -r || echo "unwired: $f"; done` returns only intentional entries.
- **Effort**: S | **Order**: quick win.

### SC-20 — Token-gate hardening bundle (strictness gaps)
- **Severity**: medium | **Category**: tests / docs
- **Motivation**: `scripts/check_manuscript_tokens.py` under-enforces in five ways: `_STEP_PHRASE_RE` (`:91`) matches only "step(s) N", so "25 steps"/"25-step pipeline" hard-typing passes; `hardcode_targets` (`:256-269`) omits `GNN_BACKEND_COUNT`, `GNN_FAMILY_COUNT`, `GNN_STEP_COUNT`, `GNN_MAINTAINED_FRAMEWORK_COUNT`, `GNN_OUTPUT_*_FIGURE_COUNT`; `@fig:`/`@tbl:` crossrefs (`:322-327`) are never checked against declared labels; the registry existence check joins an absolute `filename` verbatim (`:133`); four hand-duplicated exclusion sets (`:76-83`, `manuscript_build_figures.py:120`, two tests) drift independently. Consumer-side: the token recorder observes only `__getitem__`/`.get` (`scripts/lib/manuscript_figure_tokens.py:59-68`) — iteration/copy evades recording; the bypass guard is a textual scan (`tests/test_manuscript_figure_freshness.py:130-135`) evaded by importing `TOKENS_PATH` directly.
- **Scope**: extend the plural regex + hardcode targets; add crossref-label existence check; normalize registry filenames relative; single-source exclusion sets; recorder wraps the Mapping more completely (iteration + `in`); bypass guard checks call-graph or file-reads instead of literals.
- **Acceptance probe**: negative tests: hard-typed "25 steps" and `[@fig:typo]` in a fixture manuscript fail `--strict`; `uv run --extra dev pytest tests/test_manuscript_* tests/main/test_manuscript_* -q` green.
- **Effort**: M | **Order**: after SC-3 (regen-compare lands first).

### SC-21 — Three figures read the working tree while prose reads HEAD
- **Severity**: medium | **Category**: tests (gate machinery)
- **Motivation**: `scripts/manuscript_fig_pipeline_dag.py:161`, `manuscript_fig_family_framework.py:44-45`, `manuscript_fig_backend_matrix.py:40,57,65-67` read `STEP_INDEX.md`/manifest/registry **from the working tree**, while prose counts come from the HEAD `RepositorySnapshot` (`src/gnn/manuscript/variables.py:92-116`): dirty-tree figure-vs-prose divergence is invisible; edits to those sources without a rebuild are undetected (the freshness test itself declares the scope gap, `tests/test_manuscript_figure_freshness.py:19-26`).
- **Scope**: read those sources through the same HEAD snapshot mechanism, or record source digests in the registry and check them in the freshness gate.
- **Acceptance probe**: dirty a figure source → freshness gate fails; clean tree → passes.
- **Effort**: M | **Order**: with SC-3.

### SC-22 — LaTeX-log and published-command gates hold only as of the last committed render
- **Severity**: medium | **Category**: tests
- **Motivation**: `tests/test_manuscript_latex_log.py:34-35` and `tests/test_manuscript_published_commands.py:35-46` gate **committed** render artifacts (`output/pdf/` is tracked); no workflow re-renders (verified across all 10 workflows), so a re-rendered-but-uncommitted log/PDF is invisible. `test_manuscript_published_commands.py:39` scans only ` ```bash ` fences — `shell`/`console`/`sh` fenced commands escape the "published command must be accepted" contract.
- **Scope**: add a scheduled CI re-render job (or document the render ritual as the release step + digest-check committed artifacts); widen the fence-language set.
- **Acceptance probe**: CI job (or local script) re-renders and compares digests; fence-widening test green.
- **Effort**: M | **Order**: after SC-9 decision (what stays tracked).

### SC-23 — Producer-side fragility: `unknown` tokens, silent dirty-tree, unpinned z_ contract
- **Severity**: medium | **Category**: tests (gate machinery)
- **Motivation**: `src/gnn/manuscript/variables.py:130-134` — snapshot degrades to `GNN_GIT_COMMIT="unknown"` when git is absent, and "unknown" is a published token that reaches the PDF (only a test asserts ≠ unknown, no gate does). `scripts/z_generate_manuscript_variables.py:72-91` — dirty-tree divergence is stderr-only. `:4-6` — the "invokes this exact script name automatically" render contract lives in an external template repo, absent from this checkout; nothing in-repo pins the script name or the z_-sort position.
- **Scope**: fail (or gate-fail) on `GNN_GIT_COMMIT != HEAD` in the committed JSON (ties to SC-3); make dirty-tree a visible receipt; pin the contract with a test asserting the script name + invocation expectations documented in-repo.
- **Acceptance probe**: negative test with a dirty tree → gate fails visibly; `GNN_GIT_COMMIT=unknown` → gate fails.
- **Effort**: S/M | **Order**: with SC-3.

### SC-24 — Packaging/docs drift bundle (small, verified)
- **Severity**: medium | **Category**: docs / packaging
- **Motivation**: verified drift, each one line-ish: `pyproject.toml:265` Documentation URL → `/tree/main/doc` (no such dir; docs live in `docs/`). `pyproject.toml:78` `geo-infer` extra missing from the manual `all` list (`:216-261`) and from the MAINTAINER NOTE enumeration. `README.md:85,103` + AGENTS.md claim "32 module directories"; actual `src/gnn/` has 46. `SECURITY.md:16-22` supported-versions table tops out at 3.2.x — current release 3.3.0 absent. `.agent_rules/testing.md:20` teaches deprecated `from src.gnn import ...` (line 14 of the same file uses the correct form — internally inconsistent). `.agent_rules/dependencies.md:23` claims flax is Optional; it is core (`pyproject.toml:64`). `src/gnn/main.py:37-52` docstring cites `python src/main.py` and a nonexistent `src/README.md`. `src/gnn/pipeline/pipeline_validation.py:338` cites stale path `src/utils/pipeline_template.py`. `ARCHITECTURE.md:111-124` dependency-graph edges (TYPE→VAL, VAL→EXP, EXP→VIS, RENDER→SEC, EXEC→ANA) show zero import evidence on spot-check; `:50` lists bnlearn as first-class integration (it is render-only, `pyproject.toml:204-208`).
- **Scope**: fix each cited line; add a docs_audit invariant for the supported-versions table (version appears in SECURITY.md); correct the module-dir count to a live-derived number or drop it.
- **Acceptance probe**: `uv run --extra dev python docs/development/docs_audit.py --strict --check-anchors --no-write` green; `grep -n "tree/main/doc\b" pyproject.toml` empty; `grep -rn "32 module" README.md AGENTS.md` empty.
- **Effort**: S | **Order**: quick win.

### SC-25 — Default timeout for `utils/network_utils.py`
- **Severity**: medium | **Category**: security / reliability
- **Motivation**: `utils/network_utils.py:35` — `httpx.request(method, url, **kwargs)` has no default timeout and `batch_request` (`:75-77`, sequential) passes none: a slow endpoint hangs the step indefinitely. Every other egress point in the repo has explicit timeouts (openrouter/perplexity 300s, sympy 30s, ollama per-call).
- **Scope**: default `timeout=` parameter (align with the 300s convention) + plumb through `batch_request`.
- **Acceptance probe**: unit test asserting a hanging endpoint (mock) raises after timeout rather than blocking.
- **Effort**: S | **Order**: quick win.

### SC-26 — pytest.ini warning filters hide deprecation rot
- **Severity**: medium | **Category**: tests
- **Motivation**: `pytest.ini:115-123` blanket-ignores `DeprecationWarning`, `PendingDeprecationWarning`, `UserWarning`, `FutureWarning` suite-wide — upstream deprecations (e.g. JAX/NumPy/pytest APIs) surface only as hard breakage, and the `UserWarning` ignore hides sklearn correctness warnings. Also: `timeout=300, timeout_method=thread` (`:72-73`) cannot interrupt C extensions or subprocess waits — soft exactly where the 49 subprocess-using test files can hang (the real bound is the outer 600s wrapper); four different `maxfail` semantics coexist (`pytest.ini:59`=10, `tests/test_runner_modes.py:67,257`=5/3, `tests/categories.py`=6) making runs non-comparable and abort-at-10 hiding later failures across 4,171 tests; `asyncio_mode=auto` (`:76`) for only 6 async tests; `log_file=output/2_tests_output/pytest.log` DEBUG (`:106-109`) is written concurrently by the nested runner (`tests/test_runner_modes.py:112,114`) and couples ad-hoc runs to the output tree (SC-9).
- **Scope**: flip selected warning classes to errors per-module (ratchet via `filterwarnings = ["error", "ignore::...", ...]` starting with `src/gnn`-internal modules), keep the JAX fork RuntimeWarning ignore with its comment; move the log file under a gitignored path or `/tmp` opt-in; single maxfail constant; drop asyncio auto mode for explicit markers.
- **Acceptance probe**: `uv run --extra dev pytest tests/pipeline -q -W error::DeprecationWarning` passes (or yields a concrete, actionable deprecation list).
- **Effort**: M | **Order**: independent; after SC-10.

### SC-27 — `grammars/` + `formal_specs/`: verify-and-prune-or-wire
- **Severity**: medium | **Category**: architecture
- **Motivation**: shipped in the wheel (`pyproject.toml:291-292`) but the review found exactly **one** inbound reference (`src/gnn/mcp/gnn_root.py` — correcting the scout's "zero inbound" claim; verify what it loads before pruning). If these grammar/spec payloads have no real consumer, they are dead wheel weight; if the MCP root tool depends on them, that dependency should be explicit and tested.
- **Scope**: trace the `gnn_root.py` reference; either wire both dirs into a documented loader with a test, or remove them from the wheel and the tree.
- **Acceptance probe**: `uv run --extra dev pytest tests/mcp -q` green; `uv build` wheel size delta documented.
- **Effort**: S (verify) / M (prune) | **Order**: quick verification first.

### SC-28 — fep-lean workflow PR filter + uv version pinning
- **Severity**: medium (cost) | **Category**: CI
- **Motivation**: `fep-lean-paired-revision.yml:28` — `pull_request` with no branch filter runs the pair-checkout + uv-sync on PRs to any branch (docs-only PRs included); every other workflow filters to main. uv is floating latest in CI (`setup-uv@v7` with no `version:` input anywhere) vs pinned 0.12.0 in Dockerfile (`:12`) vs 0.12.10 local — lockfile-behavior drift risk.
- **Scope**: add `branches: [main]` to the fep-lean PR trigger; pin `version:` on `astral-sh/setup-uv` steps repo-wide.
- **Acceptance probe**: actionlint green; a docs-only PR shows no fep-lean run.
- **Effort**: S | **Order**: quick win.

### SC-29 — Environment-skip inventory + the empty-parameter-set skip
- **Severity**: medium | **Category**: tests
- **Motivation**: measured skip profile of the default suite (17): 11 sklearn + 1 torch (the TODO's known 12), plus 2 D2 CLI (`tests/visualization/test_d2_visualizer.py:262,384`), 2 Julia (`tests/render/test_stigmergic_multi_agent.py:379,393`), and 1 **empty-parameter-set** skip at `tests/render/test_framework_availability.py:160` — a parametrization that collected zero params and pytest reported as skip, which usually signals a broken fixture/registry assumption rather than an environment gap. TODO's "12 environment-skipped tests" is accurate but incomplete.
- **Scope**: fix the empty-parameter-set case (error, not skip — a parametrize over an empty registry list should fail loudly); record the full 17-skip inventory in `tests/TEST_SUITE_SUMMARY.md` with conditions.
- **Acceptance probe**: `uv run --extra dev pytest tests/render/test_framework_availability.py -q` shows 0 skips after fix; summary doc matches `-rs` output.
- **Effort**: S | **Order**: quick win.

---

## Minor items

### SC-30 — `__all__: list[Any]` → `list[str]`
- **Severity**: minor | **Category**: code-quality
- **Motivation**: `src/gnn/__init__.py:147`.
- **Scope**: retype; add to mypy strict-clean ratchet.
- **Acceptance probe**: `uv run --extra dev mypy src/gnn/__init__.py` clean. | **Effort**: S | **Order**: any.

### SC-31 — `re.compile` inside per-item functions in `manuscript/variables.py`
- **Severity**: minor | **Category**: performance
- **Motivation**: `src/gnn/manuscript/variables.py:300,650-651` — patterns recompiled per release/per line; module-level compile is the codebase norm (e.g. `maxima_parser.py:33-40`).
- **Scope**: hoist to module constants.
- **Acceptance probe**: `uv run --extra dev pytest tests/main/test_manuscript_variables.py -q` green. | **Effort**: S.

### SC-32 — Git subprocess calls without timeout in `manuscript/variables.py`
- **Severity**: minor | **Category**: reliability
- **Motivation**: `variables.py:121,179` — the only 2 of 89 subprocess sites without `timeout`; `:179` `git cat-file --batch` with stdin can hang indefinitely on a corrupt repo.
- **Scope**: add timeouts consistent with the other 87 sites.
- **Acceptance probe**: `grep -n "subprocess.run" src/gnn/manuscript/variables.py` shows `timeout=` on every site. | **Effort**: S.

### SC-33 — defusedxml fallback silently degrades protections
- **Severity**: minor | **Category**: security
- **Motivation**: `parsers/xml_parser.py:15-17`, `parsers/schema_parser.py:17-20` — `try: defusedxml except ImportError: xml.etree` with `# nosec B405`; defusedxml is a hard dependency (`pyproject.toml:49`), so the fallback is dead in healthy envs but silently unprotected in broken ones.
- **Scope**: fail loudly (log error + raise) instead of silent fallback.
- **Acceptance probe**: sabotage import in a test → clear error, not silent stdlib parse. | **Effort**: S.

### SC-34 — `execute/processor.py` continues past known-broken interpreter detection
- **Severity**: minor | **Category**: reliability
- **Motivation**: `execute/processor.py:1087-1096` — PyMDP dependency-missing detection warns "Continue anyway as it might be a local import", then runs a certain-to-fail script.
- **Scope**: make continuation opt-in (`--allow-missing-deps`) or budget-limited.
- **Acceptance probe**: negative test: missing pymdp → fast structured failure, no wasted run. | **Effort**: S.

### SC-35 — docs_audit GFM slugger approximation
- **Severity**: minor | **Category**: docs tooling
- **Motivation**: `docs/development/docs_audit.py:156-164` — approximate GitHub slug algorithm can diverge on emoji/unicode headings (rare anchor false positives/negatives).
- **Scope**: document the limitation or port a stricter slugger.
- **Acceptance probe**: `--check-anchors` green on a fixture heading with emoji. | **Effort**: S.

### SC-36 — Minor doc/tool consistency nits
- **Severity**: minor | **Category**: docs / CI
- **Motivation**: `ci.yml:12` concurrency group double-encodes the workflow name; `src/gnn/main.py:37-52` stale docstring (folded into SC-24 scope but listed for completeness); `parsers/markdown_parser.py:201` `model_name` taken verbatim from spec content — currently stem-fed consumers, but unguarded against future spec-name feeding into file paths (defense-in-depth: route through `render/naming.safe_output_stem`).
- **Scope**: one-line fixes; sanitize at the visualization-output seam (`visualization/network_viz.py:203`, `combined_analysis.py:187`).
- **Acceptance probe**: grep + existing suites green. | **Effort**: S.

### SC-37 — Test-suite summary + README counts drift
- **Severity**: minor | **Category**: docs
- **Motivation**: `tests/TEST_SUITE_SUMMARY.md` and README carry test counts; measured reality on 2026-09-09: 4,867 collected, 4,821 passed / 3 failed / 17 skipped under the default filters (SC-10 pending). Any static count rots. The summary additionally claims "10 conftest markers" (conftest registers none — `tests/conftest.py:38-42`; pytest.ini has 12) and lists `@pytest.mark.xfail` as registered (absent from `pytest.ini:83-95`; zero xfail usages in the tree).
- **Scope**: derive counts in docs from `just test-count` output or mark them with an as-of date; correct the marker claims; refresh once SC-10 lands (→ 4,824 passing).
- **Acceptance probe**: docs_audit green; numbers match a fresh collect. | **Effort**: S.

---

## Research-direction items

### SC-38 — Split `utils/` by concern
- **Severity**: medium | **Category**: architecture (research-direction)
- **Motivation**: 46-module grab-bag; own docstring admits "future pass should split it by concern" (`utils/__init__.py:6-9`); 118 re-exported names; imported by 71 files. Every deprecation cycle (SC-15, SC-17) pays a coordination tax through it.
- **Scope**: not a mechanical move — design pass first: which of the 118 names are public API (registry-documented) vs internal; propose 4-6 concern packages (`logging`, `io`, `subprocess/exec`, `safety`, `validation`); deprecation-alias migration like SC-7.
- **Acceptance probe**: design doc + migration ratchet (`uv run --extra dev pytest tests/ -q` green per moved module).
- **Effort**: XL | **Order**: after SC-15/SC-17 establish the pattern; gate for v4.0.0.

### SC-39 — Execution-stack unification (beyond the MCP bypass)
- **Severity**: medium | **Category**: architecture / security (research-direction)
- **Motivation**: three envelope shapes coexist (`execute/processor.py:execute_single_script` with gate+sandbox; `executor.py:execute_script_safely`; hand-rolled runner envelopes), plus Julia availability probing duplicated in 4 modules (`julia_env.py:72`, `julia_setup.py:46`, `activeinference_runner.py:72`, `analysis/rxinfer/cross_framework.py:163-317`). SC-1 fixes the security hole; this item is the durable consolidation.
- **Scope**: one envelope (gate → sandbox → timeout → capture → normalized errors), one availability-probe module with caching (also fixes the per-script re-probe cost at `execute/processor.py:1070-1118`).
- **Acceptance probe**: runner count calling the shared envelope = all; Step 12 + MCP suites green; probe count per pipeline run measurable and reduced.
- **Effort**: L | **Order**: depends on SC-1; rides SC-6.

---

## Test-estate deep dive (late scout, integrated 2026-09-09)

### SC-40 — Test runner lies about what ran (phantom `src/tests/`, 21% "comprehensive")
- **Severity**: major | **Category**: tests / DX
- **Motivation**: `tests/test_runner_modes.py:90-98,114` + `tests/runner.py:47,79,117-119` — the runner's default fast path invokes pytest on `src/tests/`, which **does not exist** (verified: no such dir, zero git files; the stale `.pytest_cache/lastfailed` entries with `src/tests/...` node IDs are its fossil). Fast-only errors, then auto-falls back to "comprehensive" mode — which iterates a hardcoded `MODULAR_TEST_CATEGORIES` table of **81 files vs 377 on disk** (`test_runner_modes.py:191-210`), leaving ~296 files (~3,100 test functions incl. all of `tests/render`, `tests/analysis`, `tests/visualization`) unreachable through the runner's own "all tests" mode. 32 files reference `src/tests/`. Both modes misreport coverage.
- **Scope**: delete/redirect the `src/tests/` references (they predate the 3.3.0 one-corpus restructure); make comprehensive mode discover `tests/` recursively or grow the routing table mechanically from the tree; make `--fast-only` run a real marker-based fast subset (`-m fast`) instead of falling back.
- **Acceptance probe**: `uv run --extra dev pytest tests/test_runner_modes.py -q` green; a runner "all tests" invocation collects ≥ the plain `pytest tests/` count (4,867).
- **Effort**: M | **Order**: quick win adjacent; lands with SC-10.

### SC-41 — Zero-skip contract has a token loophole with 3 live evasions
- **2026-09-09 main-delta**: **DONE on main** — "Bare-form evasions closed 2026-09-08": the contract now matches non-decorator skip forms and all evaders are allowlisted with rationale. Drop after rebase.
- **Severity**: major | **Category**: tests
- **Motivation**: `tests/test_zero_skip_contracts.py:34-41` — `FORBIDDEN_SKIP_TOKENS` matches only the literal `@pytest.mark.skipif`, so `pytestmark = pytest.mark.skipif(...)` (no leading `@`) evades it. Live evasions: `tests/execute/test_lean_runner.py:49` — **whole file silently skipped** on Lean-less machines and not in the 9-entry allowlist (`:11-31`); `tests/pipeline/test_pomdp_pipeline_integration.py:27` — `_skip_no_pymdp` indirection applied at `:160,225,295`; `tests/utils/test_shared_helpers.py:53`. Same class as SC-10 (marker-hidden behavior): the suite's guardrails themselves are guarded by string matching.
- **2026-09-09 main-delta (refined after verification)**: the three live evasions are closed on main — all evaders are in `DEFAULT_SKIP_ALLOWLIST` with rationale, and the token list gained bare `pytest.mark.skip`/`pytest.mark.xfail` + unittest forms. Residual: bare `pytest.mark.skipif` is still absent from `FORBIDDEN_SKIP_TOKENS` (main's `tests/test_zero_skip_contracts.py:58-60`), so a future `pytestmark = pytest.mark.skipif(...)` would evade the scan. After rebase, keep a one-line follow-up: add the bare skipif token.
- **Scope**: extend the forbidden-token patterns to `pytestmark =` and decorator indirections (or scan AST for `skipif` attribute access); move the 3 evaders into the explicit allowlist with rationale or convert to `importorskip`-with-reason.
- **Acceptance probe**: sabotage test — adding a `pytestmark` skipif fails the contract test; `uv run --extra dev pytest tests/test_zero_skip_contracts.py -q` green after allowlisting.
- **Effort**: S | **Order**: quick win.

### SC-42 — ~4,800 lines of tests are never collected (orphaned in-package test files)
- **Severity**: major | **Category**: tests / architecture
- **Motivation**: `pytest.ini:16-17` `testpaths = tests` only. Orphans: `src/gnn/testing/test_round_trip.py` (2,214 ln) + `test_comprehensive.py` (1,005) + `test_integration.py` (197) + `test_xml_parser_only.py` (137) = 3,553 ln inside the package, plus `src/gnn/utils/test_utils.py` (1,265 ln; only its helpers are imported by `tests/mcp.py:11`). ~3,700-4,800 lines of test code run under no CI and no local run — `test_round_trip.py` is simultaneously MAJ-04's third-largest "module" and dead test weight. (`tests/utils/` mirrors only 96+157 ln, not equivalent.)
- **Scope**: decide per file: move genuine round-trip/comprehensive tests under `tests/testing/` (then collect), or delete duplicates. Doing this **before** MAJ-04's `testing/` decomposition converts the biggest file from dead weight into the safety net for the split.
- **Acceptance probe**: `uv run --extra dev pytest --collect-only -q | tail -1` count grows by the adopted tests; zero `src/gnn/**/test_*.py` outside `tests/` (or a documented allowlist).
- **Effort**: M | **Order**: before SC-6 touches `testing/`.

### SC-43 — Weak-assertion program: 145 tautology-only tests, two mask real rot
- **Severity**: major | **Category**: tests
- **Motivation**: 145 tests whose only assertion is `is not None` / `len(x) > 0`. Worst, with rot-masking consequences: `tests/test_core_modules.py:503-514` — `except ImportError: pass … assert len(imported) >= 3`: **7 of ~10 core modules can fail to import while the test passes**; `:519-527` repeats the pattern in a perf test (crashing modules escape timing). `tests/pipeline/test_pipeline_integration.py:67` — `assert "import" in code or "def" in code or len(code) > 0` (tautology for any non-empty string) in a cross-step data-flow test whose sibling asserts (`:41,46,65`) check only object existence. Medium instances: `tests/report/test_report_integration.py:245-248`, `tests/gnn/test_gnn_processing.py:40-43` (import-smoke ×4), `tests/visualization/test_visualization_overall.py:85-121` ({} passes), `tests/website/test_website_overall.py:76,118`, `tests/execute/test_execute_pymdp_utils.py:70-75` ("should be JSON serializable" never calls `json.loads`).
- **Scope**: delete or strengthen, worst-first: core-modules import test becomes a strict `importlib` loop failing on any missing module; the pipeline data-flow test asserts real cross-step artifacts; the six "overall" files either assert observable output or are deleted. Establish the bar in `.agent_rules/testing.md`.
- **Acceptance probe**: `grep -rn "assert .* is not None" tests | wc -l` drops by ≥100; a deliberately broken module import fails `tests/test_core_modules.py`.
- **Effort**: M | **Order**: after SC-10/SC-40 (counts stabilize first).

### SC-44 — Machine-dependent runtime in the default suite
- **Severity**: medium | **Category**: tests
- **Motivation**: three cost/behavior traps: (a) `tests/llm/test_llm_ollama.py:195` — real LLM inference runs in the default suite whenever a local Ollama daemon is up (machine-dependent minutes; my verification run excluded these two files by `--ignore`, CI excludes them only in coverage contexts — see the SC-13/parity notes); (b) `tests/infrastructure/test_uv_environment.py:557-566` — runs `uv sync --frozen --check --inexact` with 120s timeout ×3 attempts + sleeps → up to ~360s inside a "unit" test; (c) `tests/execute/test_execute_overall.py:448-455` — script body `time.sleep(1000)` bounded only by executor timeout → 16.7-minute hang on regression (all other sleeps ≤5s).
- **Scope**: mark (a) with a dedicated marker excluded from default runs (align local/CI, closing the TODO parity note); (b) demote to a scheduled/labeled gate; (c) shrink the sleep to the timeout-boundary minimum that still exercises the path (e.g. 2s with a 1s executor timeout).
- **Acceptance probe**: `uv run --extra dev pytest tests/ -q -n4` wall-time stable across machines with/without Ollama; the sleep test completes in <5s.
- **Effort**: S/M | **Order**: with SC-29.

### SC-45 — Test duplication: 57 import-smoke clones + 8 fixture redefinitions
- **Severity**: medium | **Category**: tests
- **Motivation**: the same 3-test import-smoke block is copy-pasted across 7 files — `test_module_imports` ×19, `test_module_importable` ×19, `test_get_module_info` ×19 ≈ 57 functions replaceable by one parametrized fixture over the module registry. `sample_gnn_file` is locally redefined in 6 files (`tests/render/test_render_integration.py:44` + research, ml_integration, pipeline/test_pipeline_recovery, security, gui) vs the canonical `tests/conftest.py:194`; `sample_gnn_spec` in 2. `tests/render/test_stigmergic_multi_agent.py:176,233` duplicates ~70% assert scaffolding across the RxInfer/ActiveInference.jl backend classes.
- **Scope**: one parametrized import-smoke module driven by the registry; conftest-only fixtures (delete local redefinitions); extract a shared stigmergic-assert helper.
- **Acceptance probe**: `grep -rn "def sample_gnn_file" tests | wc -l` → 1; collected count unchanged (parametrization preserves coverage); suite green.
- **Effort**: M | **Order**: after SC-43 (rewrites may supersede some clones).


---

## Prioritized execution order

**Rationale**: security holes first (cheap, high-leverage), then anything that lies to developers (red suite, broken DX, false-green gates), then the big mechanical programs once the seams they touch are settled.

1. **Quick wins (S, ≤1 day each)** — SC-10 (suite red), SC-40 (runner lies), SC-41 (skip-contract loophole), SC-4 (fresh-clone DX), SC-5 (Dockerfile), SC-24 (docs/packaging drift), SC-13 (`just quality` parity), SC-11 (SHA pinning), SC-12 (full-extras false green), SC-14 (pre-commit), SC-25 (network timeout), SC-28 (workflow filters), SC-16 (version constants), SC-19 (dead scripts), SC-29 (skip inventory), SC-30–SC-37 minors.
2. **Mediums (M, 1-3 days each)** — SC-3 + SC-20 + SC-21 + SC-23 (token-gate trustworthiness, one coherent program), SC-1 + SC-2 (execution security), SC-42 (adopt orphaned tests — gates SC-6), SC-43 (weak assertions), SC-45 (test duplication), SC-44 (machine-dependence), SC-15 (utils fallback/logging), SC-17 (error hygiene tiers 1-3), SC-26 (warning ratchet), SC-27 (grammars verify), SC-9 (output/ policy — decision meeting, then mechanics).
3. **Majors (L/XL, one PR per module)** — SC-7 (validate_gnn*), SC-8 (process_*_mcp), SC-6 (six decompositions + print/logging conversion riding along; after SC-42), SC-18 (print→logging at seams), SC-39 (envelope unification), SC-38 (utils split).

**Cross-cutting note**: the branch is 231 behind `main`; rebase (and re-run the §Verification-Run table) before starting any item above — several cited line numbers will move.

---

## §Cleared-Item-Evidence (TODO truth pass, 2026-09-09)

**Cleared: 0 items.** No open roadmap item was found fully complete against the
current tree. Three items were **rewritten in place** as partially complete
(TO-DO.md), with this evidence:

1. **`setup_step_logging` residue (was: "RESOLVED 2026-09-07 where it was real")**
   — Partially true only. Verified gone: `utils/migration_helper.py` (ls: no such file). Verified still present: `src/gnn/utils/pipeline.py` — a live module with **46 inbound importers** (grep count), whose `get_output_dir_for_script` (`utils/pipeline.py:17-36`) silently re-implements `pipeline.config.get_output_dir_for_script` behind `except (ImportError, Exception)`, and whose `get_pipeline_utilities` wires `setup_step_logging` (`:72,126`). The dual logging entry (`utils/logging_utils.py` vs `utils/logging/logging_utils.py`) also remains. TODO item rewritten to the precise remaining work; detail also scoped as SC-15.
2. **Local/CI parity bullet** — Re-verified 2026-09-09: tokens + skills-health CI wiring confirmed (`.github/workflows/local-gates.yml:50-53`); `just gridworld` deliberately unwired confirmed (`local-gates.yml:8-13`). New verified facts added to the item: the CI coverage run does not ignore the Ollama tests while local `test-cov` does (`ci.yml:131-136` vs `justfile:36-38`; mitigated by self-skips — no executed-test drift); the default suite currently carries 3 marker-hidden failures for the removed `utils/migration_helper` (`tests/pipeline/test_pipeline_infrastructure.py:204`, marker at `:20`); skip inventory is 17, not 12 (SC-29).
3. **Dependency floors** — Verified in `pyproject.toml` (numpy>=2.0 :32, pandas>=2.0 :70, openai>=2.0 :45, pytest>=8.0 :91, mypy>=1.0 :108). Item rewritten to only the remaining cosmetic floors. (Also corrected the stale "87 findings" count to the measured 84.)
4. **"Recently closed" claims audited, no changes needed**: MAJ-07 (tests `tests/pipeline/test_health_check.py`, `test_pipeline_validator.py` exist), MAJ-02/03 (probe files present), GNN-02/03 (files present), GNN-04 fep_lean slice (`.github/workflows/fep-lean-paired-revision.yml`, `.github/fep-lean-pair.json` pinning `3f3100e`, `docs/development/fep_lean_paired_revision.md` — all exist; row stays open per its own acceptance note: hosted run + categorical/H3 slice outstanding). GNN-05 remains open: `7_export.py` still requires explicit `geo_step_seconds` (no notation-derived path found) — consistent with the open row.

Ledger line appended at TO-DO.md bottom per campaign contract: `Cleared 0 completed items on 2026-09-09 — per-item evidence in SCOPE-2026-09-09.md §Cleared-Item-Evidence.`
