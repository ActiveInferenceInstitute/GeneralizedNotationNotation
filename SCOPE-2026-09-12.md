# SCOPE-2026-09-12 - GNN families + custody campaign record

**Author**: orchestrator (campaign: "all further improvements, additions,
refactoring, pushed to main via informative PRs").
**Base**: main @ 82ae3efef (post toolchain-parity). Tree clean at start.
**Method**: 5 read-only scouts → 3 family-extraction workers (file-disjoint
stream branches) → serial merge + conflict resolution → custody drift-signal
worker → gate battery + full suite → paired re-pin → closeout.

## §Waves

### Scope (5 parallel scouts)
- TodoScout: full TO-DO truth audit — W2/MCP/tests+CI tables verified
  stale-landed; 5 residual rows classified; NEW finds: broken
  `model_family_acceptance.py:355` subprocess target (retired-path bug in
  src/main.py), `validation_schemas.py:136` docstring, `testing/constants.py`
  + fixtures old-path strings.
- FamiliesScout: §3.8 residual families scoped (files, consumers, cycles,
  per-family PR plans; order system_env → config_io → errors;
  simulation_utils deferred per R4).
- LoggingScout: logging single-entry contract fully scoped (one bypass site
  `base_processor.py:25`; fix = one-line repoint + one forbidden contract
  with two graph-verified ignores).
- CustodyScout: fresh-vs-committed drift-signal design (FAIL/WARN semantics
  from v1's render_inputs discriminator; NO-GO on v2 — would red test :201).
- QualityScout: gates green; 8 modules in the unscoped 1500–2000 line band;
  2 mypy knobs unflipped (strict_equality, warn_unreachable); 2 doc-only
  subpackages without tests.

### Execute (3 parallel family workers → serial git)
- errors/ (481+339 ln leaves; 5 src + 6 test consumers migrated; 96 tests).
- config_io/ (4 leaves; 5 src + 5 test consumers; 101 tests).
- system_env/ (3 leaves incl. matplotlib_setup; 4 src + 2 test consumers).
- Shared-file conflicts (pyproject I5, arg_parsing) resolved by combining
  both intents; the lost system_env _EXPORT_MAP repoints restored in a fix
  commit; ruff format applied; SKILL.md facade references repointed (caught
  by skills-health resolvability in the full suite).

### Custody stream
- `verify_fresh_render(project_root) -> list[str]` in render_custody.py
  (read-only sibling of custody_issues; shared `_input_tree_issues` helper;
  FAIL = missing/joint-drift, WARN = artifact-only drift with inputs
  matching; v1 schema kept — v2 NO-GO documented).
- `scripts/z_verify_fresh_render.py` + workflow step between render and
  re-record; 4 new tests (16 total green).
- Paired re-pin: bridge ok at 8d484c677; fep PR #10; pair-pin bump final.

## §Receipts

- Family suites: 332 tests in tests/utils green; light-import OK;
  `_EXPORT_MAP` == 113 keys exactly; lint-imports 2/2 kept.
- Full suite: 5,553 passed / 1 failed → the 1 (custody manifest
  counts_describe_commit one commit behind HEAD) fixed by SC-22 ritual at
  HEAD; 16/16 latex-log tests; skills-health green after SKILL.md repoint.
- Gate battery: ruff + format clean, mypy 0 (671 files), capability
  contracts, terminology, docs audit + anchors, doc path references, mcp
  skills-health, actionlint — green at 8d484c677.
- Token ritual at final HEAD: clean; custody manifest records 7e6073484
  content, re-recorded at 8d484c677.
- CI: Local gates green at 7e6073484/8d484c677; Paired fep_lean revision
  repaired via fep PR #10 re-seal + pair-pin bump (see paired-repin row).
