# SCOPE-2026-09-15 — GNN repo improvement spec (corrective completion of the interrupted scope pass)

**Author**: corrective scope agent (completing the scope pass the prior agent
finished probing but died on before writing this file / committing).
**Base**: local `main` @ `9848dd28f` (fep_lean source pin cycle #7) with the
prior agent's TO-DO.md truth pass applied but uncommitted (103+/116− edit,
reviewed in full and carried into this document). Nothing here is implemented.
**Method summary**: the prior agent's two probe batches were verified complete
per its digest; this pass added 8 lightweight probe batches (no suites, no
linters) to disposition the rows the digest flagged NOT-YET-DISPOSITIONED, to
re-verify the rewritten residual rows, and to scope the §Improvements items.
Counts: **14 rows cleared** (12 by the truth pass + MAJ-06 and GNN-04
dispositioned here), **7 open rows** (W2-D6, W2-M2 shrunk, W2-J1,
validate_gnn* retirement, SC-38 tail, V4-STAGE limits, paired-repin
discipline), improvements: 2 major / 3 medium / 2 minor.

## §Method (probe batches, one line each)

- Prior agent, batch 1 (per digest, verified): the 12 cleared-row probes —
  file:line checks for W2-D1..D5/D7, W2-M1/M3/M4, MED-03b, MED-04/MIN-01,
  GNN-05 — plus the 1500–2000-line band wc re-verification (main.py 1949,
  pomdp_extractor 1775, execute/processor 1543).
- Prior agent, batch 2 (per digest, verified): residual re-verification — no
  `/24` remains in `src/gnn/pipeline/health_check.py`; the W2-M2 warning half
  at `src/gnn/pipeline/config.py:210-217`; the W2-J1 site grep.
- Probe 3 (this pass): `def validate_gnn\w*` over `src/gnn` — 20 defs, of
  which 9 alias defs each emit `DeprecationWarning` (see N-2); zero
  gui-local `validate_gnn` defs remain (the 2026-09-09 count's last
  non-package def is gone).
- Probe 4 (this pass): `def process_\w*_mcp` over `src/gnn` — 19 defs (18
  module `mcp.py` wrappers + the `advanced_visualization/AGENTS.md:467` doc
  example); all 18 module wrappers delegate through `run_pipeline_step_mcp`.
- Probe 5 (this pass): GNN-04 custody state — `.github/gnn-pair.json` pins
  GEO-INFER @ `c0115779d05369c1ba63f5009f7de53e4bb3d3d5`, last touched at
  `f37cb0c6d` (wave C, 2026-09-10); `main` @ `9848dd28f` is the pin cycle #7
  fep_lean bump, i.e. the pair is current.
- Probe 6 (this pass): pyproject state — no global `F401`/`F811` ignore,
  twelve documented per-file-ignores (`pyproject.toml:448-467`: nine
  re-export surfaces + `parsers/*` + the package root + `tests/**`);
  raised floors
  numpy>=2.0 `:35`, openai>=2.0 `:48`, pandas>=2.0 `:73`, pytest>=8.0
  `:228`, mypy>=1.0 `:242`; cosmetic floors networkx>=2.6.0 `:37`,
  plotly>=5.15.0 `:74`, scipy>=1.7.0 `:102,:179`; `ml-ai` `:100` / `torch`
  `:136` extras present; `pyyaml>=6.0` core `:38`.
- Probe 7 (this pass): residual-row anchors — both live W2-J1 wrappers and
  the migrated third site (`src/gnn/export/processor.py:585-588`), the
  W2-M2 loader halves (`config.py:77-89`), the W2-D6 header
  (`health_check.py:10-17`), and the dispatcher module heads.
- Probe 8 (this pass): `utils/mcp_dispatch.py` is a `DeprecationWarning`
  shim over `utils/mcp/dispatch.py`; `git log -S run_pipeline_step_mcp`
  dates the dispatcher landing to 2026-09-07.

## §Cleared-Item-Evidence (14 cleared rows)

Per-item evidence, one line each. Lines 1–12 cite the prior agent's verified
digest (its probes; corroborated where this pass re-read the cited anchors);
13–14 are dispositions added by this pass.

- W2-D1 — validator probes package-rooted + fail-loud:
  `src/gnn/pipeline/pipeline_runtime_validator.py:68-85` (digest probe).
- W2-D2 — ontology default resolves the packaged file:
  `src/gnn/utils/arguments/pipeline_arguments.py:13-15`; zero retired
  `src/ontology` strings in src/gnn (digest probe).
- W2-D3 — zero legacy `gnn.utils.pipeline_template` imports; the numbered
  scripts import `gnn.utils.pipeline_orchestration.pipeline_template`
  directly (digest probe).
- W2-D4 — `src/gnn/execute/processor.py:558` defaults
  `require_render_summary=True` (digest probe).
- W2-D5 — `src/gnn/main.py:459-485` `_preflight_config_gate`, wired at
  `:608` (digest probe).
- W2-D7 — `tests/pipeline/test_main_wiring.py` exists; the
  `test_pipeline_overall` façade checks were deleted per the 2026-09-11
  wave (digest probe; deletion re-confirmed in TO-DO.md scope evidence).
- W2-M1 — dead registry tokens absent; registry names `_export_with_geo`,
  `src/gnn/pipeline/step_registry.py:104-105,273-275` (digest probe).
- W2-M3 — zero `src.cli`/`src.mcp`/stale-`PYTHONPATH=src`/retired
  `src/render/` strings in src/gnn (digest probe).
- W2-M4 — the row's own acceptance grep returns zero (digest probe).
- MED-03b — `src/gnn/execute/rxinfer/rxinfer_runner.py:66-69` evidence
  persistence (digest probe; corroborated by the TO-DO remainders block).
- MED-04/MIN-01 — transport limits + hygiene, landed in the 2026-09-11
  wave (digest; TO-DO remainders block re-verified 2026-09-15).
- GNN-05 — `src/gnn/export/notation_metadata.py` exists (delivered; digest
  probe).
- MAJ-06 (dispositioned this pass) — the `process_*_mcp` collapse is LANDED:
  all 18 module `mcp.py` wrappers delegate through the generic dispatcher
  `run_pipeline_step_mcp` (`src/gnn/utils/mcp/dispatch.py`, landed
  2026-09-07; `git log -S` receipt), leaving only thin per-module
  registration defs — exactly the end-state SC-8 scoped. The
  `gnn.utils.mcp_dispatch` earlier name survives only as a documented
  `DeprecationWarning` shim (part of the SC-38 facade window, see §Carried-Open).
- GNN-04 (dispositioned this pass) — paired interchange CI is DELIVERED and
  current: `.github/gnn-pair.json` pins GEO @ `c0115779…` (last touched at
  the wave-C commit `f37cb0c6d`), and `main` @ `9848dd28f` is the pin cycle
  #7 fep_lean source bump — the custody pair is sealed at the tree being
  committed.

Also confirmed RESOLVED-in-place (already marked in TO-DO.md's "Smaller
scoped cleanups"; probe-verified this pass, no row changes): the ruff
F401/F811 per-file-ignore split (`pyproject.toml:448-467`, global ignores
gone), the ml-ai/torch extras parity (extras present at `:100`/`:136`), and
the dependency floors raise (`:35,48,73,228,242` — with the documented
cosmetic-floor residual carried as improvement N-6 below). The former
top-level "Open Scoped Roadmap" table is header-only retired prose, as the
truth pass recorded.

## §Carried-Open (the 7 residual rows)

Open rows in TO-DO.md's tables, with this pass's verification state:

1. **W2-D6** (pipeline-orchestration table, Medium) — health_check error
   branches still untested (version_issues, julia subprocess failure,
   incomplete structure, limited/partial integration, scoring tiers,
   recommendations, `main()` exit codes, verbose report); the stale
   header comment at `health_check.py:15-16` remains unconfirmed. Carried
   unchanged (test-authoring work; probe cannot clear it).
2. **W2-M2** (pipeline-orchestration table, Minor) — RESHRUNK this pass:
   both behavior halves verified landed (unregistered-stem warning
   `config.py:210-217`; parse failure logs error-level with the path
   `config.py:86-88`; the dead `.py` branch is gone — loader dispatches
   `.yaml`/`.yml` vs JSON by suffix, `config.py:77-86`). Only the pinning
   tests remain.
3. **W2-J1** (pipeline-orchestration table, Major) — all three historical
   sites now route through `resolve_step_output_dir` (third-site migration
   CONFIRMED this pass: `export/processor.py:585-588` imports the helper
   and a pointer comment records the heuristic deletion), but the two
   wrapper helpers keep divergent `except ImportError` fallbacks
   (`framework_common.py:130-131` → `output_dir.parent / "12_execute_output"`;
   `runner.py:29-30` → `output_dir` unchanged). Scoped as N-1.
4. **validate_gnn* retirement** ('Still open (residuals)' table) — window
   opened 2026-09-11, SRC/DOC caller migration complete 2026-09-14,
   retirement (alias deletion) v4-gated. Probe corroborates: 9 alias defs
   across 7 modules all emit `DeprecationWarning`. Scoped as N-2.
5. **SC-38 tail** ('Still open' table) — families `errors/`, `config_io/`,
   `system_env/` extracted with DeprecationWarning facades; logging
   single-entry contract landed; `simulation_utils` resolved as
   production-dead removal. Remaining per design §8: facade
   deprecation-window end only (delete old paths, v4-gated) — this now
   includes the `gnn.utils.mcp_dispatch` shim verified in probe 8.
   Scoped as N-3.
6. **V4-STAGE limits** ('Still open' table) — consolidated executor covers
   stems {0,3,5,7,8,11}; remaining limits (in-process steps cannot be
   force-killed, stem 9 excluded, matplotlib caveat in the thread-pool
   tier) are recorded in ADR 0001. Carried unchanged; no code action is
   scoped beyond the ADR's record.
7. **paired-repin discipline** ('Still open' table) — standing closeout
   ordering (all content edits → token ritual → bridge re-pin → fep_lean
   PR/merge → pair-pin bump as the FINAL commit, single push);
   `main` @ `9848dd28f` is itself a compliant pair-bump final commit.
   Carried unchanged (process discipline, not code work).

Open-by-design residuals (explicitly not counted as rows): the local
`test-cov` Ollama-ignore asymmetry (26-test delta), `just gridworld` unwired
until Julia toolchains exist, and the cosmetic dependency floors (scoped as
N-6). Out-of-scope observations from the truth pass (PipelineContext dead
weight, AGENTS.md data-dependency overstatement, `run_session` unwired)
stand; the PipelineContext decision is scoped as N-4 below.

## §Improvements

### Major items

### N-1 — Fold `resolve_step_output_dir` fallback semantics into the helper (W2-J1 residual)
- **Severity**: major | **Category**: architecture
- **Motivation**: the shared helper `gnn.pipeline.config.resolve_step_output_dir`
  exists and all three historical call sites route through it, but the two
  wrapper helpers keep divergent `except ImportError` fallbacks:
  `src/gnn/analysis/framework_common.py:130-131` returns
  `output_dir.parent / "12_execute_output"` while
  `src/gnn/gui/runner.py:29-30` returns `output_dir` unchanged — two
  different recovery semantics for the same failure mode, silently divergent
  between analysis and GUI standalone use. The third site's migration is
  confirmed (`src/gnn/export/processor.py:585-588`).
- **Scope**: give the helper one explicit fallback policy (document the
  standalone behavior; prefer returning the caller-supplied directory or the
  registered step dir — pick one, do not keep both); repoint both wrappers to
  it (or inline them) and delete the divergent `except ImportError` blocks.
- **Acceptance probe**: unit tests pin identical resolved paths through every
  call site for `output/`, `output/7_export_output/`, and arbitrary subdirs;
  `uv run --extra dev python -m pytest tests/pipeline tests/analysis -q` green.
- **Effort**: S/M | **Order**: standalone; pairs naturally with a config seam PR.

### N-2 — Execute the validate_gnn* alias retirement (v4.0.0)
- **Severity**: major | **Category**: architecture (breaking-change window)
- **Motivation**: the deprecation window is open (row in TO-DO.md 'Still
  open (residuals)'): 9 alias defs across 7 modules emit
  `DeprecationWarning(stacklevel=2)` naming their canonical replacements —
  `src/gnn/__init__.py:152` (`validate_gnn_file`),
  `src/gnn/parsers/basic.py:267,363`, `src/gnn/processing/processor.py:342`,
  `src/gnn/llm/llm_operations.py:458`,
  `src/gnn/execute/pymdp/pymdp_utils.py:542`,
  `src/gnn/schema_validator/validator.py:834`,
  `src/gnn/validation/simple.py:166,194`. SRC/DOC caller migration is
  complete (2026-09-14 audit); zero gui-local defs remain (probe 3). The
  remaining 11 `def validate_gnn*` defs are distinct semantics and stay.
- **Scope**: in v4.0.0, delete the alias defs, their pins in
  `tests/test_validate_surface_aliases.py`, and any registry entries; keep
  the registered MCP tool names (`validate_gnn_file_mcp`,
  `validate_gnn_files_mcp`, `validate_gnn_content`) untouched.
- **Acceptance probe**: after deletion, `grep -rn "def validate_gnn" src/gnn`
  shows only non-alias defs; importing every retired name raises
  AttributeError/ImportError as designed; the full suite is green.
- **Effort**: M | **Order**: gated on the v4.0.0 release, per the window.

### Medium items

### N-3 — End the SC-38 facade deprecation window (delete compat paths)
- **Severity**: medium | **Category**: architecture
- **Motivation**: the utils concern-package split left
  DeprecationWarning facades at the old import paths (SC-38 tail row;
  design §8). Probe 8 adds a fresh member to the deletion list:
  `src/gnn/utils/mcp_dispatch.py` is a documented deprecation shim over
  `gnn.utils.mcp.dispatch`. All old paths are v4-gated deletions.
- **Scope**: enumerate every compat path in the split design §8 (plus the
  `mcp_dispatch` shim and the `pipeline_validator.py` compat module), delete
  them, and drop their pins/consumers in tests and docs.
- **Acceptance probe**: importing each old path raises ImportError;
  `uv run --extra dev python -m pytest tests/utils tests/pipeline -q` green;
  `lint-imports` 3/3 contracts stay green.
- **Effort**: M | **Order**: gated on the v4.0.0 release, with N-2.

### N-4 — `PipelineContext`: make the delete-vs-wire-in decision
- **Severity**: medium | **Category**: architecture / design decision
- **Motivation**: `src/gnn/pipeline/context.py` is production-dead — never
  instantiated by main.py or any step — while docs claim otherwise
  (`context.py:3-5`, `pipeline/AGENTS.md:148`). The removal surface is
  closed (the module, its dedicated test `tests/pipeline/test_pipeline_context.py`,
  the re-export pair in `pipeline/__init__.py`, two AGENTS.md lines), with
  zero production callers (truth-pass scope evidence). Only the decision is
  missing.
- **Scope**: decide delete (mechanical removal of the closed surface) vs
  wire-in (a design change to main.py composition); document the decision in
  the module AGENTS.md, then execute.
- **Acceptance probe**: if delete — `grep -rn "PipelineContext" src/gnn docs`
  returns only the tombstone/history references; pipeline suite green.
  If wire-in — a wiring test proves main.py constructs the context before
  step dispatch.
- **Effort**: S (delete) / M (wire-in) | **Order**: before v4.0.0 pipeline
  composition work (it changes the same seam as N-3's deletion surface).

### N-5 — health_check error-branch test coverage (W2-D6 residual)
- **Severity**: medium | **Category**: tests
- **Motivation**: the strings half landed (no `/24` remains,
  `src/gnn/pipeline/health_check.py`), but the error branches are still
  untested: version_issues, julia subprocess failure, incomplete structure,
  limited/partial integration, scoring tiers, recommendations, `main()`
  exit codes, verbose report (branch map as scoped 2026-09-08). The stale
  header comment at `:15-16` remains unconfirmed and should be settled in
  the same pass.
- **Scope**: add 8–10 monkeypatched deterministic tests in
  `tests/pipeline/test_health_check.py`; fix the header comment if stale.
- **Acceptance probe**: `uv run --extra dev python -m pytest
  tests/pipeline/test_health_check.py -q` green, all tests offline.
- **Effort**: M | **Order**: standalone; good first medium.

### Minor items

### N-6 — config.py behavior pins (W2-M2 residual) + dependency cosmetic floors
- **Severity**: minor | **Category**: tests
- **Motivation**: both W2-M2 behavior halves are verified landed
  (unregistered-stem warning `src/gnn/pipeline/config.py:210-217`; parse
  failure logs error-level with the path `config.py:86-88`; the dead `.py`
  branch is gone, `config.py:77-86`) but unpinned. Separately, the
  dependency-floors row's documented residual — cosmetic floors
  (networkx 2.6, plotly 5.15, scipy 1.7) — waits for the next deliberate
  lock refresh (`pyproject.toml:37,74,102,179`).
- **Scope**: add the pinning tests named in the shrunk W2-M2 row; at the
  next lock refresh, raise the cosmetic floors and confirm the lock
  resolves without package-pin changes.
- **Acceptance probe**: `uv run --extra dev python -m pytest
  tests/pipeline -q -k config` green; after the refresh,
  `uv lock --check` and `uv sync --frozen` stay green.
- **Effort**: S | **Order**: quick win; lock refresh rides the next release.

### N-7 — PyYAML optional-import guard silently ignores user config
- **Severity**: minor | **Category**: code-quality / reliability
- **Motivation**: `src/gnn/pipeline/config.py:13-20` makes PyYAML optional
  ("avoid hard failures during import time") although `pyyaml>=6.0` is a
  core dependency (`pyproject.toml:38`); in the guarded fallback the loader
  logs a warning and returns `{}` — silently discarding the user's
  `pipeline_config.yaml` (`config.py:78-84`). Same silent-degrade class as
  the defusedxml fallback fixed in the 2026-09-09 program.
- **Scope**: import yaml unconditionally (fail loud on a broken env) or make
  the fallback raise a config error instead of returning an empty dict;
  drop the `_YAML_AVAILABLE` branching.
- **Acceptance probe**: unit test asserting a broken-yaml environment
  surfaces a clear error (not an empty config); pipeline suite green.
- **Effort**: S | **Order**: standalone; pairs with N-6 (same file seam).

## Ledger

| Item | Outcome |
| --- | --- |
| Cleared | 14 (12 truth-pass + MAJ-06, GNN-04 dispositioned here) |
| Open rows | 7 (W2-D6, W2-M2 shrunk, W2-J1, validate_gnn*, SC-38 tail, V4-STAGE, paired-repin) |
| Improvements | 2 major (N-1, N-2), 3 medium (N-3, N-4, N-5), 2 minor (N-6, N-7) |
| Probes | 8 lightweight batches total; docs_audit strict is the only gate run |
