# Specification: Mcp

## Design Requirements
This module (`mcp`) maps structural logic to the overall execution graph.
It ensures that `Mcp` tasks resolve without runtime dependency loops.

## Components
Expected available types: No specific classes exported.
