# Recursive Model Fixtures

This directory is the default target for bounded `--autonomous` proposal-loop
acceptance commands. Autonomous mode writes candidate proposals under
`output/autonomous/` and does not edit files in this directory.
