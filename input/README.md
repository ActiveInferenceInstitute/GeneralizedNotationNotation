# Input

## Overview
This module handles `Input` components.

## Exemplar GNN set
The exemplar GNN spec files live under [`gnn_files/`](gnn_files/) (nested folders:
`basics/`, `discrete/`, `continuous/`, `hierarchical/`, `multiagent/`, etc.). The full set of
**29 exemplar GNN spec files** render to and execute under **RxInfer.jl**
(29/29 render, 29/29 execute).

## Available Members
- **Classes**: No specific classes exported.
- **Functions**: No specific public functions exported.

## Usage
Import necessary members directly to orchestrate tasks related to Input.
