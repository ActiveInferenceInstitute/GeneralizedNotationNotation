"""Exact MCP census pin shared by MCP gate tests.

The values below were read at work time (2026-09-25) from the committed
census ``src/gnn/mcp/audit_report.json``, which itself is generated by
``uv run python src/gnn/mcp/validate_tools.py``: tools_total=164,
modules_total=36, modules_errored=0, modules_loaded=36,
schema_checks_ok=164.

Regenerate rule: any PR that adds or removes a registered MCP tool or
module MUST regenerate the audit and update these constants in the same
PR, so the exact pins here and in ``audit_report.json`` move together.
"""

from __future__ import annotations

EXPECTED_MCP_TOOLS: int = 164
EXPECTED_MCP_MODULES: int = 36

CENSUS_SOURCE = "src/gnn/mcp/audit_report.json"

__all__ = ["CENSUS_SOURCE", "EXPECTED_MCP_MODULES", "EXPECTED_MCP_TOOLS"]
