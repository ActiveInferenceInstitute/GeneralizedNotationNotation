# GUI Tests

Pytest coverage for `src/gnn/gui/`.

This folder contains module-focused tests for GUI processing and Oxdraw integration.

Run:

```bash
uv run --extra dev python -m pytest tests/gui/ -q
```
