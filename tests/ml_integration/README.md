# ML Integration Tests

Pytest coverage for `src/gnn/ml_integration/`.

This folder contains module-focused tests for ML integration processing and coverage behavior.

Run:

```bash
uv run --extra dev python -m pytest tests/ml_integration/ -q
```
