# LSP Tests

Pytest coverage for `src/gnn/lsp/`.

This folder contains module-focused tests for language-server behavior and GNN editor support.

Run:

```bash
uv run --extra dev python -m pytest tests/lsp/ -q
```
