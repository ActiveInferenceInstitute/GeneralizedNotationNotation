# Extract Tests

Pytest coverage for `src/gnn/extract/`.

This folder contains module-focused tests for the headless POMDP extractor and its MCP tool surface.

Run:

```bash
uv run --extra dev python -m pytest tests/extract/ -q
```
