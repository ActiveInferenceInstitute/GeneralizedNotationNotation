# Model Registry Tests

Pytest coverage for `src/model_registry/`.

This folder contains module-focused tests for registry processing and model metadata round trips.

Run:

```bash
uv run --extra dev python -m pytest src/tests/model_registry/ -q
```
