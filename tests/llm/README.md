# LLM Tests

Pytest coverage for `src/gnn/llm/`.

This folder contains module-focused tests for provider wiring, cache configuration, and optional Ollama integration.

Run:

```bash
uv run --extra dev python -m pytest tests/llm/ -q
```
