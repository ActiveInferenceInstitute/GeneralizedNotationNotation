# Multimodel Tests

Pytest coverage for `src/gnn/multimodel/`.

This folder contains module-focused tests for the MCP dependency-graph tool wrapper: happy-path rendering, typed error paths, and registration shape.

Run:

```bash
uv run --extra dev python -m pytest tests/multimodel/ -q
```
