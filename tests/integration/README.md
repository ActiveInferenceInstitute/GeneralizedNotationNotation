# Integration Tests

Pytest coverage for `src/integration/`.

This folder contains module-focused tests for integration processing and cross-module coordination.

Run:

```bash
uv run --extra dev python -m pytest src/tests/integration/ -q
```
