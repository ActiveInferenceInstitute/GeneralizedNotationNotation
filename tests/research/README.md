# Research Tests

Pytest coverage for `src/research/`.

This folder contains module-focused tests for research processing and functional workflows.

Run:

```bash
uv run --extra dev python -m pytest src/tests/research/ -q
```
