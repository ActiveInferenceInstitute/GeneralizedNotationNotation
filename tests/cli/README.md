# CLI Tests

Pytest coverage for `src/gnn/cli/`.

This folder contains module-focused tests for CLI help, routing, and subcommand behavior.

Run:

```bash
uv run --extra dev python -m pytest tests/cli/ -q
```
