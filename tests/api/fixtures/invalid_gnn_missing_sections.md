# Fixture: GNN file missing required sections.

# Used by tests/api/test_api_parity.py to exercise the CLI-parity exit-code
# mapping: `POST /api/v1/validate` returns 200 with exit_code 2 (non-strict)
# and 400 bad_request (strict), mirroring `gnn validate` EXIT_WARNING/escalation.

## ModelName

Fixture Missing Sections
