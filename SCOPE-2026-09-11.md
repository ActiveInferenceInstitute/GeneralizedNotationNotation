# SCOPE-2026-09-11 - GNN repo execution record

**Author**: overnight execution agent (campaign: "proceed with all to do's and
improvements, push all to main").
**Base**: main @ a51f43ed4 (post-scope2 + addendum). Tree clean at start.
**Method**: 4 read-only scouts → 7-worker file-disjoint waves (edit-only) →
orchestrator gate battery per slice → one serial split-chain worker →
full-suite + 13-gate battery → closeout (token ritual, re-pin, single push).

## §Waves

### Wave 1 (parallel: 4 scouts + 4 executors)
- Scouts: V4-STAGE slices, V4-HD harness, validate_gnn*/level_rank/comment
  sites, stale W2/MED/MIN truth audit (7 FIXED / 4 PARTIAL / 4 OPEN).
- Landed: comment-polish (4 files), `_level_rank` name-fallback + loud
  ValueError (validator.py + tests), MED-T4 part 1
  (`tests/schema_validator/test_cross_format.py`, `tests/types/`), MED-T4
  part 2 (`tests/type_systems/`, `tests/testing/test_round_trip_*`).

### Wave 2 (parallel: 6 executors)
- StaleFixerS: W2-D4 default flip (`require_render_summary=True`), W2-D6
  step-count constant, W2-M4 stale command strings.
- WiringFixer: W2-D5 config-only preflight gate in `_prepare_pipeline_context`
  (`_preflight_config_gate`, main.py), W2-D7 wiring tests
  (`tests/pipeline/test_main_wiring.py`), deleted
  `tests/pipeline/test_pipeline_overall.py` façade checks.
- RxInferPersist: MED-03b rxinfer execution evidence persistence (mirrors jax:
  `{stem}_stdout.txt/_stderr.txt/_execution_log.json` on every run).
- MCPHygiene: MED-04 (HTTP body cap 10 MiB + 400/413 envelopes, stdio bounded
  read, response-size policy with truncate-notice) + MIN-01 (dead duplicate
  check, dead -32700 branch, cache deepcopy, ensure_ascii parity = False).
- V4HDIntegrate: `_run_factorized_sweep` routes through
  `execute_kronecker_factorized` (standard executor artifacts +
  `joint_materialized=False` in manifest) + sweep-level test.
- AliasDeprecate: validate_gnn* deprecation window (10 sites warn
  `stacklevel=2` + "removed in v4.0.0"), manifest canonical inversion fixed,
  docs updated, CHANGELOG §Deprecated.

### Wave 3 (parallel: 3 executors)
- V4ExecAC: slice A (in-process per-step timeout via future.result +
  `_ConsoleStreamTee` capture; subprocess-TIMEOUT receipt parity) + slice C
  (opt-in `collect_parsed_model` carrier forwarding step-3 parses to steps
  7/8; byte-identity pinned).
- V4Stems: slice B (whitelist {0,3,5,7,8,11} in
  `CONSOLIDATED_IN_PROCESS_STEMS`; per-stem parity tests on basics fixtures;
  9_advanced_viz excluded — D2 CLI shell-out).
- V4Parallel: slice D (shared `_execute_selected_step` dispatcher; parallel
  tier submits through it; mixed consolidated+subprocess runs legal;
  aggregation parity pinned).

### Utils split (serial chain)
- Step 0: golden `_EXPORT_MAP` assertions (113 keys frozen),
  `tests/tests/test_light_import.py`, import-linter (`uv add --dev
  import-linter`, 2 contracts kept).
- Steps 1-6: testing/, arguments/, pipeline_orchestration/,
  runtime_safety/, observability/, mcp/ concern packages; old paths are
  DeprecationWarning facades; `_EXPORT_MAP` values repointed (keys frozen);
  src consumers migrated in-family; lazy PEP 562 family inits where eager
  variants created import cycles (observability, runtime_safety —
  resource_manager's module-scope psutil; mcp — registry-discovery
  constraint documented).

## §Receipts

- Full suite (`-m "not ollama"`): **5,550 passed / 0 failed / 15 skipped /
  26 deselected** (two runs; second at final tree).
- Gate battery (13 gates): ruff, ruff format (0.15.12), mypy strict,
  lint-imports (2/2), validate-surface audit (10 ok / 0 bad), capability
  contracts, terminology, maintained-doc terms, docs audit strict
  (+anchors), gnn doc patterns, doc path references, mcp skills-health
  strict, actionlint — **all green**.
- Integration fixes caught by the battery: 2 import cycles (lazy inits),
  capability-contract path drift (arg_parsing → arguments/), README test-dir
  counts (40/38), 24 orchestrator line-count pins, `run_identity`
  monkeypatch path, extractor-isolation psutil block, figure/token freshness
  (ritual at final HEAD).
- Token ritual: variables → figures → render-custody manifest at final HEAD;
  `check_manuscript_tokens.py --strict` green; 22 manuscript tests green.
- fep-lean paired re-pin: `bridge status` green at final bytes; fep_lean PR
  + merge; `.github/fep-lean-pair.json` bump = final commit (discipline row).
