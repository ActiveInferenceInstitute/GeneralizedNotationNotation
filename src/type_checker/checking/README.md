# Type Checker Checking

Core structural checking package for Step 5.

This folder contains the rule and dimension-checking implementation used by `src/type_checker/processor.py`.

Run:

```bash
uv run pytest src/tests/type_checker/ -q
```
