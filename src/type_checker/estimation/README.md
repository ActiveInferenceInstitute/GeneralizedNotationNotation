# Type Checker Estimation

Resource-estimation package for Step 5.

This folder contains estimator strategies and report helpers used by the type-checker output pipeline.

Run:

```bash
uv run --extra dev python -m pytest src/tests/type_checker/ -q
```
