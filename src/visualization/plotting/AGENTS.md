# visualization.plotting

| Symbol | Location |
|--------|----------|
| `save_plot_safely`, `safe_tight_layout` | `utils.py` (also `_save_plot_safely`, `_safe_tight_layout` for tests) |

Configures matplotlib `Agg` before `pyplot` import. No imports from `core.process`.
