# Type Checker Tests

Pytest coverage for `src/type_checker/`.

This folder contains module-focused tests for type checking, dimensions, resource estimation, and reports.

Run:

```bash
uv run pytest src/tests/type_checker/ -q
```
