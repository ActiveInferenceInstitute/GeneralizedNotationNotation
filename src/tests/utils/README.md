# Utils Tests

Pytest coverage for `src/utils/`.

This folder contains module-focused tests for shared helpers, pipeline templates, schemas, and framework probes.

Run:

```bash
uv run --extra dev python -m pytest src/tests/utils/ -q
```
