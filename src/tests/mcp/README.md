# MCP Tests

Pytest coverage for `src/mcp/`.

This folder contains module-focused tests for MCP registry discovery, tool behavior, and audit checks.

Run:

```bash
uv run pytest src/tests/mcp/ -q
```
