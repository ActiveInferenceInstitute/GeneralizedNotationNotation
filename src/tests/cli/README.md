# CLI Tests

Pytest coverage for `src/cli/`.

This folder contains module-focused tests for CLI help, routing, and subcommand behavior.

Run:

```bash
uv run --extra dev python -m pytest src/tests/cli/ -q
```
