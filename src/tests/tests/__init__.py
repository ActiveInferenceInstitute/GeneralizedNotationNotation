"""Shared-plumbing regression tests owned by the tests module itself.

These pin the behavior of ``src/tests`` shared plumbing: the category routing
table, the unified ``TestRunner``, the helper modules, and the
``tests.infrastructure`` export surface. Deterministic, isolated, no network.
"""
