# Setup Tests

Pytest coverage for `src/setup/`.

This folder contains module-focused tests for setup validation and environment checks.

Run:

```bash
uv run --extra dev python -m pytest src/tests/setup/ -q
```
