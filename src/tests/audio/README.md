# Audio Tests

Pytest coverage for `src/audio/`.

This folder contains module-focused tests for sonification, SAPF paths, and audio generation helpers.

Run:

```bash
uv run --extra dev python -m pytest src/tests/audio/ -q
```
