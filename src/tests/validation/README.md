# Validation Tests

Pytest coverage for `src/validation/`.

This folder contains module-focused tests for semantic validation and consistency checks.

Run:

```bash
uv run --extra dev python -m pytest src/tests/validation/ -q
```
