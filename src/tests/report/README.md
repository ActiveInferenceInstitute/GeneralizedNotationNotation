# Report Tests

Pytest coverage for `src/report/`.

This folder contains module-focused tests for report generation, formats, and integration behavior.

Run:

```bash
uv run --extra dev python -m pytest src/tests/report/ -q
```
