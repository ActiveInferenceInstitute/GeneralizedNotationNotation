# Website Tests

Pytest coverage for `src/website/`.

This folder contains module-focused tests for static website generation and artifact discovery.

Run:

```bash
uv run pytest src/tests/website/ -q
```
