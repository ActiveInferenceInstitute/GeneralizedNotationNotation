# Render Tests

Pytest coverage for `src/render/`.

This folder contains module-focused tests for render processors, framework generators, MCP wiring, and renderer contracts.

Run:

```bash
uv run --extra dev python -m pytest src/tests/render/ -q
```
