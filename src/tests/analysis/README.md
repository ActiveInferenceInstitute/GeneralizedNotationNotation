# Analysis Tests

Pytest coverage for `src/analysis/`.

This folder contains module-focused tests for analysis processing, empty-input handling, and result extraction.

Run:

```bash
uv run pytest src/tests/analysis/ -q
```
