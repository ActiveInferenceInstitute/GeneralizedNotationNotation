# GNN Tests

Pytest coverage for `src/gnn/`.

This folder contains module-focused tests for parsers, processors, schemas, validation, and multi-format behavior.

Run:

```bash
uv run pytest src/tests/gnn/ -q
```
