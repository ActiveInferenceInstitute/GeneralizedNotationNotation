# Template Tests

Pytest coverage for `src/template/`.

This folder contains module-focused tests for template processing and scaffold contracts.

Run:

```bash
uv run --extra dev python -m pytest src/tests/template/ -q
```
