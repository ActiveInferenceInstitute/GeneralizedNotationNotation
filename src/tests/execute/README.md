# Execute Tests

Pytest coverage for `src/execute/`.

This folder contains module-focused tests for execution processors, framework runners, and script safety.

Run:

```bash
uv run pytest src/tests/execute/ -q
```
