# `gnn/utils/errors/` — README

Concern-package home (S2-33 Step 7, family 3/3). The old
`gnn/utils/error_handling.py` and `gnn/utils/error_recovery.py` paths are
DeprecationWarning facades — import from this package. Layout, invariants
(frozen `_EXPORT_MAP` values, eager §4.3.1 re-exports over stdlib-only
leaves), and gating tests are listed in [`AGENTS.md`](AGENTS.md).
